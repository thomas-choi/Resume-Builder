"""Vendored FUND model helpers, reused verbatim.

Phase 1.b consumes only ``fund_models.skills.scan_skills`` (the FUND skills
mechanism). ``fund_models.agent_base`` is present but out of scope until
Phase 4's tool-calling review node adopts ``AgentBase``.
"""
