# History

Newest first. Never delete or rewrite past entries — only append new ones at the top.

| Date | Action | Goal | Root Cause | Implementation Detail | Related Files | Test Coverage |
|---|---|---|---|---|---|---|
| 2026-07-22 | **Phase 7.b — passwordless account store + `/auth/*` routes, R6 enforced.** Added `User`/`Challenge`/`Session` schemas, `src/utils/auth_store.py`, `src/api/deps.py`, `src/api/auth_routes.py` and auth config; **business routes stay open** (that is 7.d) | Give the service real identity — email-as-user-id sign-up/sign-in with a 6-digit code (default) or magic link, no passwords — with the whole flow exercisable via the `file` mail backend, ahead of per-user data isolation | Through Phase 7.a there was still no identity: no account record, no session, no `/auth/*`. §14 settled the design; 7.b builds the account/challenge/session store and the routes on top of the 7.a mailer, with R6 ("no login before confirmation") the property most easily got wrong | **Schemas** (`schemas.py`): `User(email: EmailStr = user-id, display_email, first/last, email_verified, timestamps)`, `Challenge(email, purpose signup\|signin, method code\|link, attempts, expiry, consumed_at)`, `Session(email, expiry, last_seen_at)`, plus `SignUpRequest`/`SignInRequest`/`VerifyRequest`/`UserPublic`; `EmailStr` adds `email-validator` to `requirements.txt`. **Store** (`auth_store.py`, no FastAPI imports): `uid=sha256(normalize(email))` is the on-disk handle, the email never a path; `create_user` uses `open(...,"x")` (`O_EXCL`) as the atomic account claim (`FileExistsError` = "exists"); `mint(email,purpose,method)` returns the raw code/token to the caller and persists **only** the hash — code lookup `sha256(email:code)` with a companion `idx-{sha256(email)}.json` so a wrong guess can still find the live challenge to increment `attempts` and burn it past `AUTH_MAX_CODE_ATTEMPTS`, link lookup `sha256(token)`; `verify_challenge` checks method/purpose/expiry/consumed, `hmac.compare_digest` on the code, leaves a consumed idx so a replay is `410` not `400`; sessions are `secrets.token_urlsafe(32)` stored under `sha256(cookie)`, sliding `last_seen_at`/`expires_at` on load; every write atomic (tempfile+`os.replace`), every read missing-tolerant; in-process per-address + looser per-IP hourly send cap. **Routes** (`auth_routes.py`, a second unauthenticated `APIRouter` wired into `create_app` before the `/` static mount): `/auth/signup` (3 branches → identical `202`), `/auth/signin` (**R6**: the unverified branch mints a *signup* challenge, never signin), `/auth/verify` (`{email,code}` or `{token}` → `200 {user}`+`Set-Cookie` HttpOnly/SameSite=Lax/Secure/Path=/; `400` unknown/wrong-purpose/method or a signin proof on an unverified account, `410` expired/consumed/exhausted), `/auth/me`+`/auth/signout` behind `deps.current_user`; mail send failure → `502`, send-cap → `429`, a present cross-`Origin` → `403`. **Deviations from §14, both dependent on 7.c:** verify does *not* `mkdir data/users/{uid}` (per-user roots are 7.c) and `AUTH_ENABLED`/`SINGLE_USER_EMAIL` are not added yet | `src/models/schemas.py`, `src/utils/auth_store.py`, `src/api/deps.py`, `src/api/auth_routes.py`, `src/api/main.py`, `src/config.py`, `requirements.txt`, `.env.example`, `API-REFERENCE.md`, `OPERATIONS.md`, `PRODUCT-GUIDE.md`, `TECHNICAL-DESIGN.md` §14 | `tests/unit/test_auth_store.py` (24): identity case-folding, `O_EXCL` claim, code/link roundtrip, consumed→reject, wrong-code increments+burns so the right code stops working, one address's code can't verify another's, expiry, cross-purpose + cross-method rejection, **no raw code/token anywhere under `data/auth/`**, session create/load/delete/expiry/sliding, send cap. `tests/unit/test_auth_routes.py` (15, mailer mocked): signup 202 + code, verify flips `email_verified` + cookie, second signup verified→no new account+"already have" mail / unverified→fresh challenge, signin unknown→"no account" mail, verified signs in, **R6**: unverified signin yields no session and a hand-crafted signin challenge for an unverified account is `400`, `400`/`410` taxonomy, `/auth/me`+`/auth/signout` lifecycle, cookie flags, link mode, `502` on send failure. **276 unit tests green** (was 230). Verified end to end against `create_app()`: signup→read code from outbox→verify→`me`→signout→`me` 401, and the unverified-signin R6 path |
| 2026-07-22 | **Phase 7.a — testable email sender (`src/utils/mailer.py`) + mail config.** `async send(to, subject, text, html=None)` with `file` (default) / `console` / `smtp` backends | Give Phase 7 a way to send mail that needs no SMTP server, so every auth test and local run of the flow works offline — nothing else in Phase 7 is testable without it | The account flow's only feedback channel is email, but the repo had no mail path at all; a `file` backend that drops a complete `.eml` on disk makes the whole sign-up/sign-in flow exercisable with no mail server and no network | `mailer.send` dispatches on `config.EMAIL_BACKEND`: **`file`** builds an `EmailMessage` (`set_content(text)` + `add_alternative(html)` when given) and writes a complete `.eml` into `EMAIL_OUTBOX_DIR`, filename `{utcnow:%Y%m%dT%H%M%S%f}-{token_hex(4)}.eml` so the newest sorts **last** (operator reads the last file), dir created on demand; **`console`** logs subject+body at INFO; **`smtp`** stdlib `smtplib` over STARTTLS (honours `SMTP_STARTTLS`, `login` only when `SMTP_USER` set, `SMTP_TIMEOUT_S`), the blocking call offloaded via `anyio.to_thread.run_sync` — the same thread pattern the graph runs use, so `aiosmtplib` is deliberately not added. `send` is `async` for the async routes; `file`/`console` do trivial sync work inline. **Send failures propagate** (the 7.b route maps them to `502`); no retry/queue. Config rows added to `src/config.py`/`.env.example`: `EMAIL_BACKEND=file`, `EMAIL_FROM`, `EMAIL_OUTBOX_DIR`, `SMTP_*` (a shared `_flag` bool parser mirrors `RENDER_PDF`) | `src/utils/mailer.py` (new), `src/config.py`, `.env.example`, `OPERATIONS.md`, `API-REFERENCE.md` (note: no API change — utility only) | `tests/unit/test_mailer.py` (7): `file` writes a `.eml` that round-trips through `BytesParser` with the code in the body, two sends → two files with the newest sorting last, an HTML alternative yields a multipart message; `console` logs the body; `smtp` drives a **mocked** `smtplib.SMTP` asserting `starttls`/conditional `login`/`send_message` with no real socket, and no-login-without-user; a backend that raises propagates |
| 2026-07-22 | Phase 7 build steps 7.a and 7.b expanded into `PLAN.md` as implementable detail (planning only; no code changed) | Give a fresh session enough per-file detail to implement the mailer (7.a) and the auth store + `/auth/*` routes (7.b) directly from `PLAN.md`, without re-deriving the §14 design | `PLAN.md` stopped at Phase 6; §14 held the settled design but not a step-by-step, file-by-file build plan, so 7.a/7.b could not be picked up cold | Appended a "Phase 7 — planned" section to `PLAN.md` covering only 7.a and 7.b: scope boundary (auth flow works end-to-end via the `file` mail backend; business routes stay open until 7.d); two intentional deviations from a literal §14 (verify's `mkdir data/users/{uid}` and `AUTH_ENABLED`/`SINGLE_USER_EMAIL` both deferred to 7.c, since per-user roots don't exist in 7.b); 7.a — `src/utils/mailer.py` `async send()` with `file`/`console`/`smtp` backends, the mail config rows, and `test_mailer.py`; 7.b — `User`/`Challenge`/`Session` schemas (+`email-validator`), `src/utils/auth_store.py` (`uid=sha256(email)`, `O_EXCL` claim, code+link challenges, sessions, in-process rate limit), the auth config rows, `src/api/deps.py` `current_user`, `src/api/auth_routes.py` (`/auth/signup|signin|verify|me|signout` with R6 enforced and `502` on send failure), and the two unit-test files; plus the docs-to-update list and commit split | `PLAN.md`, `HISTORY.md` | None — planning entry, no code changed. The test plans for 7.a (`test_mailer.py`) and 7.b (`test_auth_store.py`, `test_auth_routes.py`) are specified in the new section per §14.14 |
| 2026-07-22 | Phase 7 design revised per user feedback — email-as-user-id, a 6-digit code as an interchangeable alternative to the magic link, unverified accounts locked out of login, and an explicit "path never web-exposed" guarantee (design only; no code changed) | Fold four pieces of user feedback on the §14 design into the doc before implementation: (R4) the email address should *be* the user-id rather than a separate uuid; (R5) verification may be a 6-digit one-time code emailed to the inbox and typed back, offered as an alternative to the clicked link; (R6) an account that signed up but never confirmed cannot be used to sign in; and (R3, sharpened) the per-user file path must never be visible over the network | The original §14 used a `uuid4().hex` user-id with a separate `emails/{sha256}` uniqueness index, made the magic link the only proof of receipt, and had an unverified account's sign-in *resend a sign-up link* (which still led to a session on completion but blurred the "no login before confirmation" line). None of that contradicted the requirements as first written, but the feedback pins down a simpler identity model and a second verification UX | Rewrote §14.1–14.15 in place. **Identity:** the normalized email is the user-id everywhere; the on-disk handle is `uid = sha256(email)`, a 64-hex string, so the address never appears in a directory name, log line or backup filename — which also *is* the R3 "path not web-exposed" guarantee (no endpoint returns a path; `FileResponse` streams bytes; the static mount serves only `frontend/dist`). The separate email index collapses: because `uid` is a pure function of the email, `open(auth/users/{uid}.json,"x")` is itself the atomic O_EXCL account claim. **Verification (R5):** a unified `Challenge{purpose, method}` model replaces `MagicToken`; `AUTH_VERIFY_METHOD=code` (default) mints a `secrets.randbelow(1e6)` 6-digit code stored under `sha256(email:code)` — scoped to the email so a guesser must target one address — with an `attempts` cap (`AUTH_MAX_CODE_ATTEMPTS`=5) and the per-address hourly send cap bounding brute force to ~25 guesses/hr over 10⁶; `=link` keeps the fragment-token/POST-from-page design. **R6:** sign-in on an unverified account returns the same uniform `202` but mints a *signup* challenge, never a signin credential, and `POST /auth/verify` re-checks `email_verified` before honouring any signin challenge — so no sign-in path can produce a session for an unconfirmed address. Logs now carry `uid`, not the email (the id is now PII). Config, migration (`--email` → `sha256` dir), the frontend verify screen (code box vs inbox message), the API table (`/auth/verify` takes `{email,code}` or `{token}`; `202` echoes `method`), and the test plan (code-attempt lockout, R6 no-login-until-confirmed) all updated to match | `TECHNICAL-DESIGN.md` (§14 rewritten), `HISTORY.md` | None — design entry, no code changed. §14.14 now additionally specifies: wrong-code increments/burns after the attempt cap; a code cannot verify another address; sign-in on an unverified account yields no session by any sequence until sign-up completes; and the vitest code-mode verify-screen cases |
| 2026-07-22 | Phase 7 designed — accounts, passwordless (magic-link) sign-up/sign-in and per-user data isolation (design only; no code changed) | Turn the three stated requirements — a sign-up screen (first name, last name, email + emailed verification link), a sign-in screen (email + emailed login link), and per-user file separation with no cross-user access — into a settled design before any implementation, since an isolation bug found after `data/` holds real accounts is a migration rather than a patch | Everything through Phase 6 is single-user by construction: `data/profiles/`, `data/sources/`, `data/output/` and `data/documents/` are one flat namespace, and any caller who knows or guesses a `profile_id` or `tailor_id` receives that file. There is no identity anywhere in the stack — no user record, no session, no authentication dependency on any route | New `TECHNICAL-DESIGN.md` §14. Storage becomes `data/users/{user_id}/` holding the §13 schema **unchanged** (so ingestion/tailoring/rendering code is untouched) plus a new `data/auth/` for users, an `sha256(email)`-keyed uniqueness claim, tokens and sessions; `users/` prefix rather than the literal `data/{user_id}` so account roots cannot collide with the system directories, with `user_id` a validated 32-hex uuid4. Magic links are `secrets.token_urlsafe(32)`, stored only as `sha256` (filename = lookup key, so no timing-attackable compare and a leaked `data/` yields no usable links), single-use, and bound to `signup`/`signin` by a `purpose` field; the link is `{PUBLIC_BASE_URL}/#/verify?token=…` — token in the **fragment** so it never reaches a log or `Referer`, consumed by a `POST` from the loaded page so mail scanners/Safe Links cannot burn it, and hash-routed so no SPA fallback is needed on `StaticFiles(html=True)`. Sessions are opaque server-side records in an HttpOnly/SameSite=Lax cookie — revocable, and the only scheme that works for the existing `EventSource` SSE endpoint. Enforcement is one rule: `user_id` comes only from the session, never from a path/query/form field, applied via a router-level `Depends(current_user)` so new routes are protected by default; stores take `user_id` as a required first argument so every call site is a type error until updated; `404` (never `403`) for another user's id. Three non-storage isolation gaps identified: the SSE job registry keyed on a client-supplied `job_id`, the tailoring checkpointer's `thread_id`, and unattributed logs. Also covered: anti-enumeration (uniform `202` on both auth endpoints, with an explanatory mail in the "no account" branch), rate limiting, a `file` mail backend as the default so the flow is testable with no SMTP server, `AUTH_ENABLED=false` legacy mode that still uses the per-user path so there is only one code path, an idempotent `os.replace`-based migration script, and the 7.a–7.e build order (data move before enforcement flip) | `TECHNICAL-DESIGN.md` (§14 appended), `HISTORY.md` | None — design entry, no code changed. §14.14 specifies the suites to be written: token lifecycle, sign-up/sign-in branches, the R3 two-account isolation suite (A requesting each of B's ids → 404, plus a parametrized "every business route 401s unauthenticated" test so an unprotected new route fails CI), session/cookie behaviour, mailer backends, migration idempotency, and the vitest cases for the gate, verify screen and `401`-vs-network-failure handling. `PLAN.md`, `API-REFERENCE.md`, `OPERATIONS.md` and `PRODUCT-GUIDE.md` are deliberately unchanged — no endpoint, env var, setup step or user-facing behaviour exists yet; they are updated as 7.a–7.e land |
| 2026-07-22 | Fix `.gitignore` silently excluding `frontend/src/lib/` from version control | Get the review UI's `api.ts`, `diff.ts` and `types.ts` into the repository — a fresh clone could not build the frontend without them | Found while checking what Phase 6.c had changed: `frontend/src/lib/api.ts` was edited but never appeared in `git status`. The stock Python `.gitignore` line `lib/` is **unanchored**, so it matches a directory named `lib` at *any* depth — including `frontend/src/lib/`, which holds the three modules every panel imports. They have been untracked since Phase 4; earlier HISTORY entries list `frontend/src/lib/diff.ts` as a related file for changes that were therefore never committed. Nothing else in the repo was affected (`git status --ignored` reports only this directory and `.claude/`) | `lib/` → `/lib/` and `lib64/` → `/lib64/`, anchoring them to the repo root where the Python packaging artifacts they were written for actually appear, with a comment recording why. Deliberately minimal: the other unanchored packaging patterns (`build/`, `dist/`, …) are left alone — `frontend/dist/` is *meant* to be ignored and is listed explicitly at the bottom of the file. The three source files now show as untracked and are ready to be committed | `.gitignore`, and newly visible: `frontend/src/lib/{api,diff,types}.ts` | None — repository configuration; the modules themselves are covered by the existing vitest suites (`api.test.ts`, `diff.test.ts` and every panel test that imports them) |
| 2026-07-22 | Phase 6.c — a failed *refresh* no longer erases a loaded profile; query defaults and transport errors made honest | Stop a one-second network blip from destroying a profile the user has on screen and may have edited, and make the next such report diagnosable without a devtools session | Three causes behind one symptom. (1) `ProfilePanel` checked `query.isError` **before** `draft`, so any failure — including a background refetch that React Query keeps the previous `data` through — replaced the whole panel; the in-file comment justified the ordering by the first-load case only, and that case is `isError && !draft`. (2) The query defaults contradicted the comment above them: `staleTime: 0` marks a response stale on arrival so every new observer of `["profile", id]` refetches — and there are two, `ProfilePanel` and `TailorPanel` — `refetchOnReconnect` was left on so any online/offline flicker refired the query, and `retry: false` made the first blip instantly fatal. (3) `Failed to fetch` is a bare `TypeError` from `fetch`, i.e. a transport failure, never an HTTP status — but it surfaced verbatim, reading as a bug in the page. The transport failure itself did **not** reproduce against either deployment shape (headless-Chrome replay: one request, 200, panel stable at t+4s), so it is environment-specific | `ProfilePanel`'s fatal branch is now `query.isError && !draft` (still ahead of the loading branch, or a first-load failure spins for ever); with a draft in hand the failure renders as a `role="alert"` banner above the still-editable profile with a Retry calling `query.refetch()`. `TailorPanel` gets the same for its `profileQuery`, conditioned on `!profileQuery.data` — a failed refetch still has the last copy to diff against, but with *nothing* loaded it used to draw an empty comparison table, which reads as "nothing changed" rather than "the comparison is missing". `main.tsx`: `staleTime: 30_000`, `refetchOnReconnect: false`, `retry: 1`; freshness still comes from the save path's explicit `invalidateQueries`, which ignores `staleTime`. `lib/api.ts` wraps a caught `TypeError` as `Could not reach the API (<path>) — is the server running?` and rethrows everything else untouched, so React Query's own cancellations are not relabelled as network failures; `getProfile`/`getReview` take and forward React Query's `AbortSignal`. **Step 5 of the plan remains open**: it needs a Network-tab capture from the reporter to decide whether a `vite.config.ts` proxy timeout/error handler is also required | `frontend/src/panels/ProfilePanel.tsx`, `frontend/src/panels/TailorPanel.tsx`, `frontend/src/main.tsx`, `frontend/src/lib/api.ts`, `frontend/src/__tests__/{ProfilePanel,TailorPanel,api}.test.*`, `PLAN.md`, `TECHNICAL-DESIGN.md`, `PRODUCT-GUIDE.md`, `API-REFERENCE.md`, `OPERATIONS.md` | 5 new vitest cases → **66 vitest green** (from 61); 230 Python unit tests untouched and green. New: a profile that loads and then fails on an invalidation-driven refetch keeps its fields, its unsaved headline edit and its conflicts under a "Could not refresh … showing the last loaded copy" banner, and Retry re-issues the request (3 calls); a first-load failure still renders the fatal "Could not load" message; `TailorPanel` names the missing comparison and retries it; `api.test.ts` — a `TypeError` becomes the "could not reach the API" message, an HTTP 404 still reports FastAPI's `detail`, and the abort signal reaches `fetch` |
| 2026-07-22 | Phase 6.a — a "Clear everything" button that empties the whole screen | Give the user a way to start a fresh session without reloading the browser, and make sure it leaves nothing of the previous one behind | There was no reset path at all short of a page reload, because every piece of session state is component-local and unreachable from the parent: `App` owns the profile ids, `SourcesPanel` the staged CV/LinkedIn files, GitHub username, token, free text, target profile id, progress and warnings, `ProfilePanel` its `draft` and save mutation, `TailorPanel` the job post, flags and result. A "cleared" prop would have to be threaded through four components and a dozen `useState`s, and would silently rot as panels gain fields | `App.tsx` holds a `sessionKey` counter and renders the panels with keys derived from it, so bumping it makes React unmount and remount the subtree — one line that discards all of the state above and cannot drift. The handler also resets `profileId`/`profileIdInput` and calls `useQueryClient().clear()`: without that the remounted `ProfilePanel` redraws the previous profile instantly from the query cache, and a Clear that leaves the old data on screen is worse than none. Guarded by `window.confirm` because it is destructive (unsaved profile edits, a staged upload set, a typed token). Clearing the GitHub token comes free with the remount — `SourcesPanel` deliberately keeps it in component state only. The button sits in the header beside the profile loader, `type="button"`, inside a new `.controls` row that reuses the loader form's flex rule | `frontend/src/App.tsx`, `frontend/src/styles.css`, `frontend/src/__tests__/App.test.tsx`, `PLAN.md`, `TECHNICAL-DESIGN.md`, `PRODUCT-GUIDE.md`, `API-REFERENCE.md` | New `App.test.tsx`: with `confirm` stubbed true, typed free text + a loaded profile are gone and the inputs empty after the click (including the profile name, proving the cache was cleared and not just the components remounted); with `confirm` stubbed false nothing changes |
| 2026-07-22 | Phase 6.b — clear the previous profile's output when a new profile becomes active, and the ingest form when a run succeeds | Stop the screen showing one profile's data under another profile's id, and stop a second "Build profile" click silently re-ingesting the same files | Three separate stale-state paths. (1) `ProfilePanel` sets `draft` only when new data *arrives*, so between the id changing and the fetch landing it renders the **old** profile's headline, summary and conflicts under the **new** id. (2) `TailorPanel.result` was never reset on a profile change, so the tailored CV, the bullet diff, a pending review and the download links stayed visible — and those links carry the old `tailor_id`, so they download the previous profile's CV. (3) `SourcesPanel` kept the staged files after a successful run, so clicking "Build profile" again re-ingested the same CVs into another profile | Split by what the state is for. The downstream panels are **keyed on the profile they describe** — `key={"profile-"+sessionKey+":"+profileId}` / `key={"tailor-"+…}` in `App.tsx` — so a new profile id remounts both, fixing (1) and (2) together with no `useEffect` reset to keep in sync and no window where old data is drawn under a new id. The key prefixes matter: two children of the same parent may not share a key, and without them React duplicated/omitted the panels (caught by the first test run, which showed the profile surviving a Clear). For (3), the inputs clear in `SourcesPanel`'s mutation `onSuccess` — staged CVs, LinkedIn exports, free text, GitHub token and the "add to an existing profile" id — never on click, so a **failed** run keeps everything staged rather than making a 500 cost the user every file they picked. The progress list and the outcome banner (including the skipped-repo list) deliberately survive the clear: they describe the run that just finished | `frontend/src/App.tsx`, `frontend/src/panels/SourcesPanel.tsx`, `frontend/src/__tests__/SourcesPanel.test.tsx`, `frontend/src/__tests__/App.test.tsx`, `PLAN.md`, `TECHNICAL-DESIGN.md`, `PRODUCT-GUIDE.md`, `API-REFERENCE.md` | 6 new vitest cases → **61 vitest green** (from 55); 230 Python unit tests untouched and green. New in `SourcesPanel.test.tsx`: staged files/free text/token/profile id are empty after a successful ingest; all of them survive a failed one; the skipped-repo banner and its "1 skipped" headline survive the clear. New in `App.test.tsx`: a tailored CV and its download link rendered for profile A are gone once profile B is loaded, and the job post box is empty |
| 2026-07-22 | Phase 6.d — draw the profile sections the UI never rendered: projects, education, certifications and contact, plus the tailored CV's selected projects | Stop a profile that ingested correctly from looking empty, and stop a person approving a document containing projects they were never shown | `ProfilePanel` rendered only name, headline, summary, conflicts, experience and skills — `projects`, `education`, `certifications` and `contact` had **no markup anywhere in the frontend**, though `data/profiles/github-only/v2.json` holds 56 GitHub projects that Phase 5.c put there correctly. The same blindness reached the tailored CV: `TailoredCV.selected_projects` is validated (`validation.py:113`), removable at review (`review.py:201`) and written into the `.docx` (`docx_renderer.py:140`), yet `TailorPanel` never displayed it. A GitHub-only ingest therefore presented as a failed one — exactly the indistinguishability Phase 5.c set out to remove | `ProfilePanel` gains Projects (name linked to the repo when `url` is set, `rel="noopener noreferrer"`, description, technologies, source), Education, Certifications and a Contact line. Two constraints shaped it: `education` is `list[dict]` with **no** guaranteed keys, so `EducationEntry` draws familiar keys first (`degree`, `field`, `school`, `institution`, `location`, dates) and lists whatever else the entry carries rather than dropping it — `location` earned its place from the real CV extractor output; and 56 items would bury the Save button, so `ExpandableList` shows `PREVIEW_LIMIT` = 10 with a "Show all *n*" toggle held in the section's own state. `TailorPanel` draws `selected_projects` between the bullets and the skills, matching `render_cv`'s section order so the review screen and the approved document agree. `diff.ts` gains `diffProjects`, matching by lower-cased name — the same key the gate and the review node use, so a project the UI marks flagged is the one the server flagged, never a client-side near-miss; dropped profile projects are named. Verified in headless Chrome against the stored profiles, not fixtures: `github-only` → "Projects (56)", 10 shown, toggle expands to 56, repo links present; `CVs-Only` → 6 education entries, 3 certifications, 4 contact fields | `frontend/src/panels/ProfilePanel.tsx`, `frontend/src/panels/TailorPanel.tsx`, `frontend/src/lib/diff.ts`, `frontend/src/styles.css`, `frontend/src/__tests__/{ProfilePanel,TailorPanel,diff}.test.*`, `frontend/src/__tests__/testUtils.tsx`, `PLAN.md`, `TECHNICAL-DESIGN.md`, `PRODUCT-GUIDE.md`, `API-REFERENCE.md` | 12 new vitest cases → **55 vitest green** (from 43); 230 Python unit tests untouched and green. New: projects listed with links/technologies and a no-URL repo still listed; the 10-of-12 preview and its toggle; the muted empty-projects placeholder; education entries with unexpected keys; certifications; selected projects on the tailored CV with the "Left out:" line; a flagged project marked; and `diffProjects` kept/flagged/dropped, case-insensitive matching, and ignoring flags of other kinds |
| 2026-07-22 | Phase 6 planned — investigated four UI defects reported after the Phase 5 run and wrote the fix plan (no code changed yet) | Turn four symptom reports into diagnosed root causes before touching the frontend, so the fixes land on causes rather than on what the screen appeared to do | Investigation, not a single cause. **(a/b) No reset path and no clear-on-rebuild:** every piece of session state is component-local (`App` owns the ids, `SourcesPanel` the staged files/token/free text, `ProfilePanel` the draft, `TailorPanel` the result), so nothing can clear it from above; `ProfilePanel` also replaces its draft only when new data *arrives*, and `TailorPanel.result` is never reset, so a new profile id renders the previous profile's fields and the previous run's download links (pointing at the old `tailor_id`). **(c) "Failed to fetch" after a good load:** verified the backend is healthy (`GET /profile/CVs-Only` → 200, 31,829 bytes) and replayed the load in headless Chrome over CDP against both the API-served bundle and the Vite dev server — exactly one request, 200, panel stable at t+4s, so the load path is sound and the transport failure is environment-specific. The defect that *is* ours: `ProfilePanel` tests `query.isError` before `draft`, so a background refetch failure that React Query keeps `data` through still erases a loaded, possibly edited profile — and the query defaults (`staleTime: 0`, `refetchOnReconnect` on, `retry: false`) make such refetches both frequent and instantly fatal, contradicting the "nothing is refetched behind their back" comment. **(d) GitHub repos invisible:** `data/profiles/github-only/v2.json` holds 56 `projects` from `github:thomas-choi`, but the frontend has no markup for `projects`, `education`, `certifications` or `contact` anywhere; `TailoredCV.selected_projects` is likewise validated, review-removable and written into the `.docx` while never being shown, so a person approves a document containing projects they were never shown | Plan only. Phase 6 added to `PLAN.md` as 6.a–6.d with the evidence above, the fix per file, a sequencing note (6.d → 6.b → 6.a → 6.c, since 6.a's remount key subsumes 6.b's and 6.c's panel edits conflict with both), a per-item test list, and one open item that needs the reporter: a Network-tab capture of the failing request to decide whether 6.c also needs a `vite.config.ts` proxy timeout/error handler. Verification bullet for Phase 6 added alongside the earlier phases | `PLAN.md`, `HISTORY.md` | None — planning entry; the tests each item ships with are named in `PLAN.md` §6 |
| 2026-07-22 | Phase 5.c — extract GitHub sources a batch of repos at a time, isolate failures to a single repo, and report every skipped repo to the caller and the UI | Stop one bad repo costing a whole GitHub source, and stop a run that lost a source from reporting itself as a clean success | The Phase 1.e resilience has a hole exactly where GitHub sits. One `github_username` renders **one** `SourceDocument` holding every repo, so item-level salvage only helps when the model returns a *parseable but invalid* tool call; when it returns a message with **no tool call at all** — which ~49k chars in, ~50 repos of structured output out, at `temperature=0.9` made likely — there is nothing to salvage from and the source-level net can only drop the entire document. Run `ui-mrvl80oa-udexcp` lost 50 repos that way, wrote a profile with 0 projects, and still returned HTTP 200 with a success banner; the log said only `structured output failed to validate …: None` | Four parts. **Split:** `github_client.split_repo_sections` cuts a rendered document into one `RepoChunk` per `### Repository:` section and `render_repo_document` reassembles any subset; the spans tile the document so `render(split(text)) == text` byte-for-byte, which is what makes pruning safe to trust. Both live beside the rendering they invert. Every chunk carries its `## …` tier heading, because `skills/source-extraction/SKILL.md` decides ownership-vs-contribution from exactly that labelling — batching without it would have re-opened the fabrication risk Phase 1.f closed. **Batch, then isolate:** `_extract_github` sends `GITHUB_REPOS_PER_EXTRACTION` (default 10) repos per call and retries a failed batch **one repo at a time**, so blame lands on a specific repository; per-batch results merge by concatenating list fields with first-non-empty `name`/`headline`/`contact`, and only an all-repos failure raises. **Prune the archive:** `github.json` is rewritten to the repos that reached the profile with the as-fetched document preserved as `github.raw.json` (own manifest entry, `#as-fetched`) — pruning without keeping the original would destroy the evidence needed to investigate the drop. The rewrite lives in `store_profile`, which already owns run-store I/O, fed by the new `IngestionState.pruned_sources`, so `extract_source` stays a pure LLM step. **Report:** `IngestionState.source_errors` → `/ingest` response + `warning` SSE events + a "Skipped N" list in the UI, with the success banner qualified. Diagnostics rewritten: a tool-call-less response now logs `finish_reason`, usage and a content preview instead of `: None`. `extract_one` now returns an `ExtractionResult` (extraction + errors + pruned text). **Synthesis is explicitly left alone** — it has the same output-size fragility one stage later, and is not fixed speculatively | `src/tools/github_client.py`, `src/agents/extraction.py`, `src/agents/ingestion_graph.py`, `src/utils/run_store.py`, `src/api/routes.py`, `src/config.py`, `.env.example`, `frontend/src/panels/SourcesPanel.tsx`, `frontend/src/lib/api.ts`, `frontend/src/lib/types.ts`, `TECHNICAL-DESIGN.md` §3/§4/§13, `API-REFERENCE.md`, `OPERATIONS.md`, `PRODUCT-GUIDE.md` | `test_github_client.py` — exact round trip, every chunk carries its tier heading, a tier whose repos all failed disappears, a document with no repos yields no chunks, and a README's own `##`/`###` lines cannot fake a boundary (they are quoted). `test_extraction.py` — a 10-repo document goes out as 4+4+2 with headings intact and no repo duplicated across batches; a single-batch document re-renders byte-identically to a plain call; a poisoned repo takes down its batch, is isolated, and 5 of 6 repos still extract with the pruned text excluding only it; all-repos-fail raises; merge keeps first identity and concatenates lists; the no-tool-call path logs finish_reason/usage/content. `test_graphs.py` — a dead source becomes a `source_errors` entry, per-repo errors propagate, `store_profile` writes both archives and indexes the raw one, and an unpruned run writes no second copy. `test_api.py` — `source_errors` in the response plus one `warning` event per item; `[]` on a clean run. `SourcesPanel.test.tsx` — skipped list renders (repo *and* whole-source rows), banner says "2 skipped", clean run shows no list, live SSE warnings render mid-run. 230 unit + 43 vitest green; verified end-to-end with only the LLM faked (30 repos, 1 poisoned → 29 in profile, `github.json` 29 / `github.raw.json` 30) |
| 2026-07-21 | Phase 5.b — keep every staged file: accumulate picks in the UI, and stop same-named uploads overwriting each other server-side | A second CV must never vanish. Found in the same end-to-end run as 5.a: picking `CV.docx` and then `CV.pdf` in two dialogs left only the second | Two independent defects with one symptom. **UI:** `SourcesPanel` did `setCvFiles(Array.from(event.target.files ?? []))` on every `change` — a file input reports only the files chosen in *that* dialog, so replacing the list discarded everything picked before. **Backend:** `run_store.save_source_file` wrote to `sources/{run_id}/{category}/{filename}`, so two uploads named `CV.docx` overwrote each other; quieter but worse, `routes._load_upload` derived `doc.id` from the *uploaded* filename, so the two also collided as source ids and `CareerProfile.raw_source_map` — what the anti-fabrication gate reads to trace a claim to its document — could no longer tell them apart | `save_source_file` suffixes a taken name via a new `_free_path` helper (`CV.docx` → `CV-2.docx` → `CV-3.docx`) and returns the path it actually wrote; its docstring now warns that the returned name is not always the one passed in. `_load_upload` / `_load_linkedin_export` build `doc.id` from `stored.name`, so archive and traceability map cannot disagree about how many sources there were. `SourcesPanel` merges each pick into the staged list keyed on `name+size+lastModified`, renders them via a new `StagedFiles` component with per-file remove buttons, and clears `event.target.value` after reading so re-picking a removed file fires `change` again — the pick is read into a local *before* the clear, since the state updater runs afterwards (the first attempt did it the other way and the accumulate test caught it). Applied to the `linkedin_export` input too. No API signature change: both fields already took `list[UploadFile]` | `src/utils/run_store.py`, `src/api/routes.py`, `frontend/src/panels/SourcesPanel.tsx`, `TECHNICAL-DESIGN.md` §3/§10, `API-REFERENCE.md`, `OPERATIONS.md`, `PRODUCT-GUIDE.md` | `tests/unit/test_run_store.py::test_save_source_file_keeps_both_files_when_names_collide` (three same-named saves → three files, three contents); `tests/unit/test_api.py::test_ingest_keeps_two_same_named_cvs_apart` (two archived files, source ids `cv_docx:CV.docx` / `cv_docx:CV-2.docx` reaching the graph and the manifest, second CV's text proving it is not the first parsed twice — `tests/conftest.py::build_sample_docx` gained a `name` argument for that); `SourcesPanel.test.tsx` (picks accumulate and all reach `ingest`; remove drops one and re-picking re-adds it; LinkedIn input behaves the same); new `frontend/src/__tests__/api.test.ts` asserts both staged files go out under the `cv` field. 213 unit + 40 vitest green |
| 2026-07-21 | Phase 5.a — accept a per-request `github_token` on `POST /ingest`, demoting `GITHUB_TOKEN` to a fallback | Let one running server ingest several people, each with their own GitHub credential, without an `.env` edit and a restart | `_headers` built auth from the module-global `config.GITHUB_TOKEN`, read once at import, and `_viewer_login` used the same global to decide whether the token was a "self-token" (which unlocks private repos and private org memberships). One process therefore served exactly one token, and exactly one user could ever have their private work read | The token became a parameter: `fetch_github_profile(username, client=None, token=None)` threads it through `_headers`, `_viewer_login`, `_graphql`, `_graphql_contributions` and `_gather_evidence`, resolving `token or config.GITHUB_TOKEN` at the top so env-only deployments behave exactly as before. `POST /ingest` gains a `github_token` form field, normalized to `None` when blank and passed via `functools.partial` (`anyio.to_thread.run_sync` takes no kwargs). The 1.g safety property is preserved rather than changed — the `GET /user` identity check now runs per request instead of per process, so user B's token unlocks B's private repos while a token belonging to neither party still never reaches a viewer endpoint. Handling obligation, stated explicitly in the docs and asserted in a test: the token is absent from `manifest.json`, from the archived `github.json`, from every log line (`github[%s]: token viewer=%s` logs the resolved login, the non-secret part), and from `localStorage`. UI: a `type="password"` "GitHub token (optional)" field in `SourcesPanel`, held in component state only and appended to the `FormData` only when non-empty, with helper text explaining that a token for the username above also unlocks their private repos while anyone else's only raises rate limits | `src/tools/github_client.py`, `src/api/routes.py`, `frontend/src/panels/SourcesPanel.tsx`, `frontend/src/lib/api.ts`, `TECHNICAL-DESIGN.md` §3, `API-REFERENCE.md`, `OPERATIONS.md`, `PRODUCT-GUIDE.md` | `tests/unit/test_github_client.py` — explicit token overrides `config.GITHUB_TOKEN` (asserting the GraphQL bearer is the caller's and the env one appears in no request), an explicit third-party token still never touches a viewer endpoint, and the tokenless path is unchanged (no GraphQL at all). `tests/unit/test_api.py` — the form field reaches `fetch_github_profile`, and the secret appears in no response and in no file under `data/` (every archived file is searched); a blank field falls back to `None`. `SourcesPanel.test.tsx` — the field is `type="password"`; `api.test.ts` — `github_token` is omitted from the body when blank and trimmed when present |
| 2026-07-21 | Make the review UI's dev server bind address configurable (`UI_HOST`/`UI_PORT`, plus `UI_ALLOWED_HOSTS`) and document setup + run commands for the frontend | Let the UI be developed against an API on another machine *and* be opened from another machine's browser — the common case here being a Linux dev box serving a Windows browser — without editing `vite.config.ts` or remembering Vite's flags | `vite.config.ts` already parameterised the proxy **target** (`API_URL`) but hardcoded nothing for the server's own address, so it always bound Vite's default `localhost:5173`: reaching the dev UI from another host meant `npm run dev -- --host`, which is undocumented here and which also silently binds every interface. The docs compounded it by showing only `npm run dev` and `API_URL=…`, two variables that look interchangeable but answer different questions (where it listens vs. where it forwards) | Frontend tooling only — no Python change. `frontend/vite.config.ts` reads `UI_HOST` (default `localhost`, so the loopback-only default is unchanged and nothing is exposed by upgrading), `UI_PORT` (default `5173`) and an optional comma-separated `UI_ALLOWED_HOSTS`, which is only needed for a DNS name since Vite's host check already permits plain IPs. `strictPort: true` is set deliberately: the configured port is the one other machines were told to use, so a collision must fail loudly instead of drifting to `5174`. The same object is reused for `preview`, so a LAN smoke test of the built bundle needs no extra flags — and because Vite's `preview.proxy` falls back to `server.proxy`, preview proxies the API paths too (verified, not assumed). Docs: `OPERATIONS.md` gains a rewritten "Developing the UI against a running API" with a four-row variable table, worked `UI_HOST=0.0.0.0 UI_PORT=3000` examples, the equivalent CLI flags, and an explicit statement that these are shell variables read by `vite.config.ts` — **not** part of the backend `.env` — plus a pointer from the main env-var table; `README.md` gains a "Running the review UI in development" section; `CLAUDE.md`'s command block shows the configurable form; `TECHNICAL-DESIGN.md` §13 records why the address is configuration rather than code (production has no Vite server at all) | `frontend/vite.config.ts`, `OPERATIONS.md`, `README.md`, `CLAUDE.md`, `TECHNICAL-DESIGN.md`, `API-REFERENCE.md` (one-line note: no API change), `PRODUCT-GUIDE.md` (one-line note: no user-facing change) | No new automated test — the change is dev-server configuration, outside the vitest (jsdom) and pytest surfaces. Verified by running it: `UI_HOST=0.0.0.0 UI_PORT=4321 npx vite` served 200 on both `127.0.0.1:4321` and the LAN address `192.168.0.212:4321`, and `UI_PORT=4322 npx vite preview` served the built bundle on the custom port **and** proxied `/healthz` to the API (`{"status":"ok"}`), confirming the preview proxy inheritance the docs now claim. `npx tsc --noEmit` clean; 33 frontend vitest tests and 206 unit tests still green |
| 2026-07-21 | **Phase 4 — Review UI + human-in-the-loop.** Added a `human_review` `interrupt()` node with a checkpointer, two resume endpoints, an `AgentBase` review agent, and a React/Vite/TanStack three-panel UI served by FastAPI from a multi-stage image | Make design doc §11's "human review checkpoint before final render" a property of the pipeline rather than a convention the client is trusted to honour, and give the whole flow (sources → profile → tailor → review → download) a browser | Through Phase 3 the render gate returned flags and `render_skipped` and asked the caller to re-run with `approve_flagged`. Two holes: a client that ignores the flags renders anyway (the guarantee was advisory), and the re-run *re-tailors* — so the CV that renders is a different draft from the one a person reviewed, at the cost of a second set of LLM calls. There was also no UI at all, so conflict resolution and flag review existed only as raw JSON | **Graph:** two new nodes between `validate_cv` and rendering — `prepare_review` (builds the `ReviewRequest`, writes the brief, archives `review.json`) then `human_review` (nothing but `langgraph.types.interrupt()` and the decision handling), split because LangGraph re-runs an interrupted node **from the top** on resume, so a single node would pay for the brief twice — and the graph compiles with a module-level `MemorySaver` (shared across compiled instances, `thread_id = tailor_id`, so a pause outlives the request that created it). It is a no-op unless `render` was requested **and** flags exist **and** the caller didn't pre-approve, so the Phase 3 `approve_flagged` path is unchanged. The cover-letter conditional edge moved from `validate_cv` to `human_review`, so the letter can only draw on claims that survived review. **Decision handling** (`src/agents/review.py`): `build_review_request` enriches each `ValidationFlag` with a stable id and the closest sourced profile claim + its source; `apply_decision` keeps approved items (retained in `flags` for provenance) and removes the rest — from the structured fields *and* from the `summary`/`headline` prose, after a live run showed a rejected skill surviving in the summary ("proficient in … Docker") once removed from `highlighted_skills`; sentences naming a rejected term are dropped, a headline naming one falls back to the profile's. Items absent from `approvals` count as **not** approved. **Review agent:** `ReviewAgent(AgentBase)` — the Phase 1.b deferral — loads `skills/` via `_load_skills`, receives FUND's runtime `load_skill_from_fs` through `register_tool`, and runs a bounded tool loop (`REVIEW_MAX_TOOL_ITERATIONS`) to write an advisory brief; `get_llm` is overridden onto `make_llm` (model tiering, provider switch, one mock point, and `AgentBase.get_llm` would send a `temperature` current Claude models reject). Any failure yields `""` — the items gate rendering, never the prose. **API:** `/tailor` now threads a thread-id config, returns `review_required`/`review`/`review_url`, and gains `GET /tailor/{id}/review` (pending items; falls back to the archived `review.json` with `pending: false`) and `POST /tailor/{id}/resume` (`409` when nothing is pending). **Frontend:** `frontend/` (React 19 + Vite + TanStack Query, TypeScript) with Sources (SSE progress, subscribes before POSTing), Profile (draft editing + conflict resolution) and Tailor (bullet diff + `ReviewPanel` defaulting every item to Remove) panels; served at `/` by `StaticFiles` when `FRONTEND_DIR` has an `index.html`, else `/` still redirects to `/docs`. **Docker:** multi-stage (`node:20-slim` build → python runtime), plus a new `.dockerignore`. **Schema:** `ReviewItem`/`ReviewRequest`/`ReviewDecision`, and `Conflict.resolution` (`str \| None`) so a resolved conflict is *recorded*, not deleted. **Config:** `REVIEW_MODEL`, `REVIEW_AGENT_ENABLED`, `REVIEW_MAX_TOOL_ITERATIONS`, `FRONTEND_DIR`. **Known gaps documented:** pending reviews die with the process (in-process `MemorySaver` → `409`; the request itself is archived), and the gate still never inspects summary/headline prose | `src/agents/review.py` (new), `src/agents/tailoring_graph.py`, `src/api/routes.py`, `src/api/main.py`, `src/models/schemas.py`, `src/utils/document_store.py` (`save_review`/`load_review`), `src/config.py`, `src/chains/prompts/review_prompt.py` (new), `skills/cv-review/SKILL.md` (new), `frontend/**` (new), `Dockerfile`, `.dockerignore` (new), `.gitignore`, `.env.example`, all five docs | `tests/unit/test_review_flow.py` (new, 18): request ids + closest-claim, approve/reject/silence/approve-all, whole experience+project removal, prose scrubbing (word-boundary, employer term, no-op when approved), interrupt fires + persists `review.json`, resume renders the pruned CV (asserted in the rendered .docx), the brief is written exactly once across pause+resume (pins the two-node split; verified to fail with the single-node shape), cover letter written only after review, clean/no-render runs never pause. `tests/unit/test_review_agent.py` (new, 9): skills loaded, `load_skill_from_fs` registered + returns a body, tool loop with a `ToolMessage` round trip, Anthropic content blocks, failure/disabled/budget-exhausted → `""`, missing `SKILLS_DIR` degrades. `test_api.py` +8 (pause response, review 404/archived fallback, resume decision + `409`, unsafe id, UI-serving root). `test_graphs.py`: the render-gate test now asserts the pause; a new test asserts `approve_flagged` still skips it. `test_skills.py` +`cv-review`. **205 unit tests green** (was 166). Frontend: `frontend/src/__tests__/` — 33 vitest tests over the diff helper and all four panels (a failing one caught a real bug: a failed profile load rendered a permanent spinner). **Verified against the running container**: flagged run paused with a 3-item brief, `GET /review` served it, resume keeping 2 of 3 rendered `cv.docx`+`cv.pdf` with the rejected skill absent from both the skills line and the summary, second resume → `409` |
| 2026-07-21 | Rewrite `API-REFERENCE.md`'s `POST /tailor` worked example as the full six-step `curl` walkthrough from job description to downloaded CV | Give the JD → targeted-CV path a copy-pasteable form, mapped one-to-one onto the pipeline steps documented the same day, so the shell commands and the internals are readable as one story | The worked example still described the Phase 1/2 world: it stopped at the JSON response, and the Phase 3 render + download commands had been appended under **Variants** as an afterthought — so the *default* documented path produced no document, the approval re-run existed only as a prose sentence with no command, and nothing connected a shell step to the node it triggers | Documentation only — no code change. Worked example restructured into six numbered steps (1 ingest → 2 save the JD to a file → 3 tailor **with `render`/`cover_letter`** → 4 read the result → 5 approve only if flagged → 6 download), preceded by a table mapping each shell step to the pipeline steps it triggers (step 3 = nodes 0–6, step 6 = node 7). Step 3's per-flag table gains rows for `render`/`cover_letter` and a note that the call is synchronous and costs 2 LLM calls + 1 per strict-checked claim + 1 for the letter; step 4's `jq` projection now also prints `rendered` and `render_skipped`; step 5 is a new `jq -e`-gated re-run with `approve_flagged` that warns a fresh `tailor_id` is minted and the LLM calls repeat; step 6 downloads all four artifacts and explains a 404. Added an "all six steps in one block" runnable script. The old render/download block and its prose were removed from Variants (now redundant), and a **JSON-only** variant was added there since the main path now renders. `OPERATIONS.md` smoke test gains a pointer to the walkthrough, marking its own commands as liveness one-liners | `API-REFERENCE.md`, `OPERATIONS.md` | No test change — documentation only. Every documented command was executed verbatim against the container before commit: the six-step happy path on profile `thomas-main` (`render+cover_letter` → `needs_review: false`, four documents rendered, all four downloaded — `cv.pdf` 4 pages, `cover-letter.pdf` 1 page, both `%PDF`), the JSON-only variant (`documents: []`, `render_skipped: "rendering not requested"`, `cover_letter: null`), and the step-4/step-5 `jq` gates driven from a synthetic flagged response to prove the flag branch fires and the clean branch stays silent. Cross-document anchors verified programmatically. 166 unit tests green |
| 2026-07-21 | Document the JD → targeted-CV path end to end in `TECHNICAL-DESIGN.md` §13 and `PRODUCT-GUIDE.md` | Let a reader follow one job description from the HTTP request to a downloadable file in one place, instead of reassembling it from five stage descriptions and a curl example | The docs covered the path only in pieces: §6–§9 describe each stage in isolation (and predate the implementation), §13's graph diagram names the nodes without saying what they read or write, and `API-REFERENCE.md`'s worked example is the *caller's* steps. Nothing stated the order of operations, which step costs an LLM call, or where a run can stop — the three questions asked when a `/tailor` run doesn't produce what was expected | Documentation only — no code change. New `TECHNICAL-DESIGN.md` §13 subsection "JD → targeted CV: one request, step by step": a Mermaid **sequence** diagram (caller → route → profile_store → graph → provider → document_store, including the optional cover-letter block and the download) plus a 8-row step table (step 0 request acceptance through step 7 download) giving module, model + skill, and in→out for each. Adds three things the per-stage sections never stated: (a) the validation gate's three layers spelled out cheapest-first — exact `raw_source_map` hit → difflib ≥ `VALIDATION_SIMILARITY_THRESHOLD` → single-claim LLM cross-check — with the deterministic membership checks for skills/experiences/projects called out as never reaching the model; (b) the three places a run stops (404/400 before anything runs; flags blocking step 5 while still returning the full CV; `render: false` no-op) and the per-request LLM-call count; (c) two easily-wrong assumptions — `raw_source_map` is excluded from the tailoring prompt so the generator never sees the index it will be scored against, and the cover letter is **not** re-validated (step 3 runs before step 4 and only over the `TailoredCV`), though its rendering is still blocked by the CV's flags. `PRODUCT-GUIDE.md` §3 rewritten from 3 bullets into the same five-step flow in user language, ending at the render gate and noting the AI-call cost; `API-REFERENCE.md` `POST /tailor` gains a cross-reference to the new section | `TECHNICAL-DESIGN.md`, `PRODUCT-GUIDE.md`, `API-REFERENCE.md` | No test change — documentation only; the described behavior is already covered by `test_graphs.py` (node order + conditional cover-letter edge + render gating), `test_validation.py` (the three layers), `test_document_agent.py` (stop conditions) and `test_api.py` (404/400, response shape). Suite re-run: 166 unit tests green |
| 2026-07-21 | Phase 3 — Document agent: `.docx`/PDF rendering + optional cover letter (PLAN.md Phase 3, design doc §9 and §1) | Turn the tailoring pipeline's JSON into the artifacts a person actually sends: a rendered CV file and, optionally, a cover letter — without letting an unvalidated claim become a polished document | The pipeline stopped at `TailoredCV` JSON. Design doc §1 promises "a tailored CV (and optionally a cover letter)" and §9 a document agent that renders it; neither existed, so every caller had to lay the CV out by hand, and `CoverLetter` was a schema with no producer | **Rendering (no LLM):** `src/tools/docx_renderer.py` renders `TailoredCV` → `.docx` in fixed schema order (name/contact header, headline, summary, experiences, projects, skills; internal `relevance_notes` omitted, empty sections skipped) and `CoverLetter` → `.docx` (header, greeting, body, closing, signature), on top of an optional base template (`DOCX_TEMPLATE`) supplying styles — a template missing `Heading 1`/`List Bullet` degrades to bold/plain paragraphs instead of failing. `convert_to_pdf` shells out to headless LibreOffice with a throwaway `-env:UserInstallation` profile (HOME is not reliably writable in a container); a missing/failing/timed-out binary logs a WARNING and returns `None`, so `.docx` is the guaranteed output. **Gate:** `src/agents/document.py` owns `skip_reason` — nothing renders while `validation.needs_review` is true unless the caller passes `approve_flagged`, and nothing renders if the gate never ran. **Graph:** `tailoring_graph` gains a conditional edge after `validate_cv` (→ `write_cover_letter` only when asked) and a terminal `render_document` node; state gains `tailor_id`, `render`, `want_cover_letter`, `approved`, `cover_letter`, `documents`, `render_skipped`. **Cover letter:** `tailoring.generate_cover_letter` (`COVER_LETTER_MODEL`, defaults to `TAILORING_MODEL`) is passed the profile, requirements **and** the tailored CV — the facts already deemed relevant — and composes the new `cover-letter` skill with `anti-fabrication`, per the Phase 1.b pattern. **Storage:** `src/utils/document_store.py` (third store, keyed by `tailor_id`) fixes filenames per (kind, format) so a download can never address a path outside the run's directory, and saves `tailor.json` beside the files for traceability. **API:** `POST /tailor` gains `render` / `cover_letter` / `approve_flagged` and returns `tailor_id`, `cover_letter`, `documents` (with download URLs) and `render_skipped`; new `GET /document/{tailor_id}?kind=&format=`. **Docker:** `libreoffice-writer` added to the image | `src/tools/docx_renderer.py` (new), `src/agents/document.py` (new), `src/utils/document_store.py` (new), `src/chains/prompts/cover_letter_prompt.py` (new), `skills/cover-letter/SKILL.md` (new), `src/agents/tailoring_graph.py`, `src/agents/tailoring.py`, `src/api/routes.py`, `src/config.py`, `Dockerfile`, `.env.example`, `PLAN.md`, `CLAUDE.md`, `TECHNICAL-DESIGN.md`, `API-REFERENCE.md`, `OPERATIONS.md`, `PRODUCT-GUIDE.md` | New `tests/unit/test_docx_renderer.py` (section content + ordering by re-opening the .docx, `relevance_notes` never rendered, empty sections omitted, cover-letter order, template used / missing-template fallback, PDF disabled / no-LibreOffice / command-shape), `tests/unit/test_document_agent.py` (gate matrix: pass → render, flags → skip, flags+approved → render, no validation → skip, render not requested → skip; CV-only vs CV+letter output), `tests/unit/test_document_store.py` (fixed filenames, unknown kind/format, traversal-safe id, result roundtrip), new `tests/integration/test_pdf_render.py` (real LibreOffice; skips unless the binary actually runs). Extended `test_tailoring.py` (cover letter sees profile+requirements+CV, composes both skills, degrades without skills), `test_graphs.py` (cover-letter branch taken only on request, render gated by flags), `test_api.py` (flags threaded into the graph, download URLs, skip reported, letter persisted to `tailor.json`, `GET /document` serve/404/400), `test_skills.py` (six shipped skills). **166 unit tests green** |
| 2026-07-21 | Replace the `POST /tailor` one-liner with a worked, step-by-step `curl` example in `API-REFERENCE.md` | Make the targeted-CV call copy-pasteable by someone who has not read the source, and explain *why* each part of the command is there rather than just showing it | The previous example was a single dense `curl … "$(jq -n …)" | jq` line: it assumed `$PROFILE_ID` and `job.txt` already existed (never showing how to obtain either), gave no reason for the `Content-Type` header or the `jq --rawfile` trick, and offered no guidance on reading the validation flags — the one output the caller must act on | Documentation only — no code change. New "Worked example — end to end" subsection: **Step 1** obtains `PROFILE_ID` from a real `POST /ingest` (`-F cv=@resume.docx -F github_username=…`, id extracted with `jq -r`) and notes it is reused across job posts; **Step 2** writes the posting to `job.txt` via a quoted `<<'EOF'` heredoc, explaining that unquoted heredocs expand `$`/backticks in the posting; **Step 3** the tailor call writing to `tailored.json`, followed by a per-piece table (`-s`, `-X POST`, the **required** `Content-Type: application/json` — body is a Pydantic model, not a form, so omitting it yields 422 — `jq -n --arg/--rawfile` JSON-escaping the multi-line posting, `-o` preserving the response since a re-run costs another LLM call, and the projection `jq`); **Step 4** a `jq -e` gate on `validation.needs_review` that prints each flag as `[kind] item → reason (similarity)`. Plus three variants: pinning `version`, a `for` loop tailoring one profile to a directory of postings (ingestion not repeated), and a short inline-JSON one-liner marked as smoke-test-only because it breaks on newlines/quotes. Closes with a pointer to `/docs` (Swagger) for callers who would rather not hand-build JSON | `API-REFERENCE.md` | None — documentation only. Form field names (`cv`, `github_username`, `profile_id`) verified against `src/api/routes.py:118-125`, response keys against `routes.py:292-297`, and flag fields against `ValidationFlag` in `src/models/schemas.py:123-130`. Unit suite untouched (128 passed as of Phase 2) |
| 2026-07-21 | Document `POST /tailor` (targeted-CV generation) in full in `API-REFERENCE.md` | Make the endpoint that generates a JD-targeted CV from a stored profile usable without reading `src/models/schemas.py` — it is the product's headline capability (design doc §12 step 3, "the actual value-add") but had the thinnest reference entry | The section listed request/response fields as bare schema *names* (`JobRequirements`, `TailoredCV`) with no expansion, so a caller could not see what comes back, could not tell that `selected_experiences`/`selected_projects` are a subset of the profile rather than new entries, and had no worked example. It also never stated the synchronous, no-SSE nature of the call or which error codes apply | Documentation only — no code change. Rewrote the `POST /tailor` section: named the graph path (`analyze_job → tailor_cv → validate_cv`) and its synchronous, profile-is-loaded-not-re-ingested semantics; request fields as a table (`profile_id`, `job_post`, optional `version`); explicit error codes (404 unknown profile/version, 400 empty `job_post`, 422 malformed body); a fully expanded 200 response with every field of `JobRequirements`, `TailoredCV` (incl. nested `Experience`/`Project` shape) and `ValidationResult`/`ValidationFlag`; notes that selections are a profile subset carrying their `source` id, that `relevance_notes` is internal and not rendered, and that `needs_review` traces to `raw_source_map` with review left to the caller until Phase 4; a `curl`+`jq` example; and a pointer that rendering/cover letter (Phase 3) and server-side approval (Phase 4) are not yet available | `API-REFERENCE.md` | None — documentation only; the documented contract is already covered by the existing `POST /tailor` tests in `tests/unit/test_api.py` and the tailoring/validation suites. Verified the documented fields against `src/models/schemas.py` (`JobRequirements`, `TailoredCV`, `Experience`, `Project`, `ValidationFlag`, `ValidationResult`) and `src/api/routes.py:271-297` (`TailorRequest`, error paths, response keys). Unit suite untouched (128 passed as of Phase 2) |
| 2026-07-21 | Phase 2 — LinkedIn data-export ingestion (`POST /ingest` accepts `linkedin_export`) | Bring LinkedIn — the one career source most people keep most current — into the profile, and let a CV-vs-LinkedIn disagreement about the same job surface as a `Conflict` instead of being invisible | Phase 1 had no LinkedIn path at all: the design doc (§3) ruled out scraping (ToS, no personal-app read API), and PLAN.md Phase 1.a routed the "LinkedIn" input through the generic `free_text` field, so a person could only paste a summary by hand — losing structure (dates, employers, titles), losing every section the export carries (education, certifications, recommendations), and giving synthesis nothing to detect a conflict against | New `src/tools/linkedin_export.py`: pure-Python, offline parse of the official export ZIP (or a single CSV from it) into **one** `SourceDocument` per upload — `structured_fields` holds the exported rows grouped into `profile`/`positions`/`education`/`skills`/`certifications`/`recommendations_received`, `raw_text` a deterministic Markdown rendering of the same. Two real-export quirks drove the parser: section files are matched on a **normalized stem** (`Recommendations_Received.csv` ≡ `Recommendations Received.csv`), and the header row is **located by its columns** rather than assumed to be line 1, because LinkedIn prefixes several CSVs with a free-text `Notes:` preamble. Blank cells are dropped so an absent field stays absent (Phase 1.e contract). No recognized section / corrupt ZIP → `ValueError` → HTTP 400, after the upload is archived (archive-before-parse, as for CVs). `extraction_prompt` gained a `{structured}` slot filled only when `structured_fields` is present, telling the model the exported records are authoritative over the rendered text — prose sources keep a byte-identical prompt, so no graph branch and no regression. `skills/source-extraction/SKILL.md` gained a "Structured exports" section holding the two attribution rules: profile skills are self-asserted (never promoted to achievements) and recommendations are third-party statements (never restated as the person's own claims). `POST /ingest` takes `linkedin_export` file(s), archived under `data/sources/{run_id}/linkedin/`, category `linkedin` in the manifest. No new env vars, no new dependencies (`zipfile`/`csv` are stdlib) | `src/tools/linkedin_export.py` (new), `src/chains/prompts/extraction_prompt.py`, `src/agents/extraction.py`, `src/api/routes.py`, `skills/source-extraction/SKILL.md`, `tests/conftest.py`, `TECHNICAL-DESIGN.md` §3/§4/§13, `API-REFERENCE.md`, `PRODUCT-GUIDE.md`, `OPERATIONS.md`, `PLAN.md` | New `tests/unit/test_linkedin_export.py` (10 tests: every section parsed; `Notes:` preamble skipped; blank cells absent, not `""`; rendered sections/date ranges; single-CSV upload; filename variants; unknown members ignored; no-section, unsupported-type and corrupt-ZIP errors). Extended `test_extraction.py` (structured records reach the prompt marked authoritative + the export skill section is in the system prompt; a prose source gets **no** structured block), `test_synthesis.py` (CV-vs-LinkedIn start-date disagreement dedupes to one experience and stays in `conflicts`), `test_api.py` (export upload → parsed linkedin source in the graph + archived/indexed under the run; unsupported extension → 400; corrupt export → 400 with the upload still on disk). `pytest tests/unit/ -v` → 128 passed, no regressions |
| 2026-07-21 | Phase 1.g — private org membership + "access is not contribution" filter for GitHub ingestion | Make the organization tier both complete and honest: find the organizations the user actually belongs to (Phase 1.f found none for the reference user), and stop rendering repos they were merely invited to | Phase 1.f was designed and verified against *public* API responses, which hid two facts. (1) **`GET /users/{u}/orgs` lists only public memberships, and private is GitHub's default** — it returned `[]` for `thomas-choi` while he belongs to 5 orgs (`MachineLearningTrade`, `EnsembleTechs`, `Neural-Matrix-LLC`, `NEURAL-TECH-PTE-LTD`, `Alpha-Wing`). The knock-on effect was worse than a gap: unable to see the affiliation, the client had classified 14 repos of the user's **own companies** as "external contributions *not owned by the user*", inverting the attribution and understating his role. Phase 1.f's premise that `GITHUB_TOKEN` is a third-party operator credential was also simply false here — `GET /user` shows the configured token *is* `thomas-choi`'s, with `repo` scope. (2) **`affiliation=collaborator` returns every repo the user was ever invited to**: 88 for this account, of which a per-repo commit probe proved he had committed to exactly 1. Rendering those would have handed the extractor 87 other people's projects to mine for achievements | **Fix 1 (self-token path):** `_viewer_login` resolves `GET /user` on every run; when the token's login equals the ingested username the client switches to the viewer endpoints — `/user/orgs` (private memberships) and paged `/user/repos?affiliation=owner,organization_member,collaborator` (private repos) — via new `_list_repos`/`_fetch_orgs(is_self)`. The identity check is the safety property: a token for any other login never touches a viewer endpoint, so a third party's private data stays unreachable. `GITHUB_INCLUDE_PRIVATE=false` drops private repos while still discovering memberships; included private repos render `Visibility: private (cannot be linked or shown publicly)` so tailoring never offers one as a portfolio link. Memberships line is labelled "Organization memberships" on the self path, "Public organization memberships" otherwise. **Fix 2 (contribution probe):** new `_keep_contributed` filters non-owned repos to those with an actual commit — repos already proven by the merged-PR search or GraphQL commit counts pass free, the rest cost one `GET /repos/{full}/commits?author={u}` each, bounded by `GITHUB_MAX_CONTRIBUTION_PROBES=150`; unproven repos are dropped, never assumed, and a 403/429 stops the sweep with a WARNING instead of failing the ingest. The GraphQL commit map was evaluated as the sole filter and **rejected**: it found 8 of the 26 truly-contributed repos (it counts default-branch commits under a matching account email only) versus 26/26 for the REST probe. `_fetch_external_contributions` was split into `_gather_evidence` (called once, shared by both consumers) + pure `_external_contributions`. **Fix 3 (org breadth):** new `_rank_org_repos` caps survivors at `GITHUB_MAX_ORG_REPOS=20` newest-first but seeds one repo per organization before filling by recency — a straight recency sort silently evicted two 2021-era employers (`NEURAL-TECH-PTE-LTD`, `Alpha-Wing`) in favour of more repos from the current one, and a resume needs breadth of employers more. New env: `GITHUB_INCLUDE_PRIVATE=true`, `GITHUB_MAX_CONTRIBUTION_PROBES=150`, `GITHUB_MAX_ORG_REPOS=20`; API contract unchanged | src/tools/github_client.py (module docstring, `_viewer_login`, `_paged`, `_fetch_orgs`, `_list_repos`, `_keep_contributed`, `_gather_evidence`, `_external_contributions`, `_rank_org_repos`, `_render_repo`, `fetch_github_profile`), src/config.py, .env.example, TECHNICAL-DESIGN.md §3 (new "Membership privacy & the contribution probe" subsection), OPERATIONS.md (3 env rows + rewritten "GitHub coverage and rate limits"), PRODUCT-GUIDE.md (GitHub paragraph + 2 limitation rows), API-REFERENCE.md (no-API-change note) | tests/unit/test_github_client.py +8 (19 total), all on `httpx.MockTransport`: org repo without commits dropped, merged-PR evidence skipping the probe, self-token using `/user/orgs`+`/user/repos` (the handler *raises* if a public endpoint is touched) and flagging private repos, `GITHUB_INCLUDE_PRIVATE=false` excluding private repos, a third-party token never reaching a viewer endpoint (handler raises on `/user/*`), 403 on the commit probe still returning owned repos, org cap by recency, and org cap preserving one repo per employer. Existing Phase 1.f tests updated for the new contract (handler now serves `/user` 401 + commit probes). Full unit suite **112 passed** (was 104), no regressions. Live-verified with `fetch_github_profile("thomas-choi")`: all 5 org memberships now named (was 0), 20 org/collaborator repos across 5 employers rendered from 66 candidates (was 2 repos), 18 private repos included, and the 14 bogus "external" repos correctly reclassified into their owning orgs — the external section is now legitimately empty for this user. Container `/ingest` re-run still pending |
| 2026-07-21 | Phase 1.f — GitHub ingestion beyond personally-owned repos | Capture the work a candidate did inside organizations and as a contributor to other people's repos — for many engineers the majority of their real engineering output — which was entirely invisible to the profile | `fetch_github_profile` called `/users/{u}/repos?type=owner` (`src/tools/github_client.py:44`), which by definition returns only repos under the personal username. Verified live: a representative user had 31 owned repos and **0** external ones by that call, while actually having 3 public org/collaborator repos and 1052 merged PRs into repos like `pallets/flask`, `pypa/pip`, `readthedocs/readthedocs.org` | **Fix 1:** `type=owner` → `type=all`, partitioned by `repo["owner"]["login"]` into owned vs. org/collaborator, plus `/users/{u}/orgs` (best-effort, non-fatal) to distinguish *member of* from *collaborator on*; forks still excluded. **Fix 2:** external contributions via `_fetch_external_contributions` behind one internal function with two providers — always the REST merged-PR search (`GET /search/issues?q=author:{u}+type:pr+is:merged`, single page, aggregated per repo into a count + up to 8 titles), and when `GITHUB_TOKEN` is set additionally GraphQL `repositoriesContributedTo(includeUserRepositories:false)` for repo metadata plus `contributionsCollection.commitContributionsByRepository` **looped per year** (it defaults to the last 12 months only), summed. Owned/org repos and repos owned by the username are excluded from this tier so nothing is double-counted. `GET /search/commits` stays rejected (counts forks/mirrors). **Fix 3 (anti-fabrication):** the source document is restructured from bare `## Repository: <name>` headings into labelled sections — `## Owned repositories`, `## Organization repositories (member of\|collaborator on <org>)`, `## Contributions to external repositories (not owned by the user)` — external entries are marked `(owned by others)`, carry `Contribution scope: N merged PRs; M commits` + PR titles, and get **no README excerpt** (a README describes the project, not the contribution); `skills/source-extraction/SKILL.md` gains an "Ownership vs. contribution" rule. Found during live verification and fixed here: README excerpts are quoted line-by-line (`> `) because a README's own `##` headings otherwise appear as section boundaries of the document (a live run showed 17 README headings sitting alongside the 4 real ones), and contribution scope is pluralized ("1 commit", not "1 commits"). **Fix 4 (budget):** languages/README fetched only for owned+org repos, external repos ranked by contribution volume (not `updated`) and capped by `GITHUB_MAX_EXTERNAL_REPOS`, search paged once, 403/429 on search logs a WARNING and degrades to owned+org rather than failing the ingest. Private/internal org repos remain **out of scope** (they need the *user's own* token; `/ingest` takes a bare username against an operator credential) — documented in OPERATIONS/PRODUCT-GUIDE. New env `GITHUB_INCLUDE_CONTRIBUTIONS=true` + `GITHUB_MAX_EXTERNAL_REPOS=15`; API contract unchanged | src/tools/github_client.py, src/config.py, .env.example, skills/source-extraction/SKILL.md, TECHNICAL-DESIGN.md §3, OPERATIONS.md (env table + new "GitHub coverage and rate limits" section), PRODUCT-GUIDE.md, API-REFERENCE.md (no-API-change note), PLAN.md | Rewrote tests/unit/test_github_client.py over the existing `httpx.MockTransport` (no network): mixed owned/org `type=all` payload + `/users/{u}/orgs` + a `/search/issues` page — 11 tests covering the three section headings and their order, org attribution (member vs. collaborator), external repo showing merged-PR count + titles with **no README excerpt** (the transport asserts external repo detail is never fetched), forks still excluded, 403-on-search still returning owned+org repos, `GITHUB_INCLUDE_CONTRIBUTIONS=false` skipping search entirely, ranking + capping by contribution volume, own/org repos not double-counted as external, and a token case stubbing the GraphQL POST (contributed-repo list used, per-year commit windows summed). tests/unit/test_extraction.py +1: a real GitHub document with an external section reaches the extractor with both the labelled sections and the skill's ownership rule in the prompt. Full unit suite **104 passed** (was 93), no regressions across Phases 1–1.e. Live-verified against the real API with `fetch_github_profile("thomas-choi")`: the document now carries exactly the 4 expected `##` headings, 2 collaborator orgs (`trgordonb`, `thebillmar`) and 14 external repos with per-repo commit/PR scope — none of which the old `type=owner` call could see. Container `/ingest` re-run still pending |
| 2026-07-21 | Design + roadmap Phase 1.f — GitHub ingestion beyond personally-owned repos — **design only, not implemented** | Capture the work a candidate did inside organizations and as a contributor to other people's repos, which for many engineers is the majority of their real output but is currently invisible to the profile | `fetch_github_profile` calls `/users/{u}/repos?type=owner` (`src/tools/github_client.py:44`), which by definition returns only repos under the personal username. Verified live against a representative user: `type=owner` → 31 repos, **0** external, while the same user has 3 public org/collaborator repos and 1052 merged PRs into repos like `pallets/flask`, `pypa/pip`, `readthedocs/readthedocs.org` | Documented (no code change), after verifying every endpoint live. **Fix 1:** `type=owner` → `type=all` (returns owner + member), partitioned by `repo["owner"]["login"]` into owned vs. org/collaborator repos, plus `/users/{u}/orgs` to name the organizations; forks stay excluded. **Fix 2:** contributions to repos the user neither owns nor is a member of, via two providers behind one internal function — with `GITHUB_TOKEN`, GraphQL `user.repositoriesContributedTo(includeUserRepositories:false)` (all-time, with description/language/stars) plus `contributionsCollection.commitContributionsByRepository` looped per year (⚠️ it defaults to the **last 12 months** only); without a token, REST `GET /search/issues?q=author:{u}+type:pr+is:merged` aggregated per repo. Either path yields a contribution scope ("6 merged PRs; 41 commits") + PR titles. `GET /search/commits` **rejected** — counts forks/mirrors, reported 57,068 commits for a user with 778 real commit contributions. **Fix 3 (anti-fabrication):** today every repo renders as a bare `## Repository: <name>` with description + README excerpt, which applied to `pallets/flask` reads as *the user's project*; the source document is restructured into three labelled sections (`Owned` / `Organization (member of <org>)` / `Contributions to external repositories (not owned by the user)`), external repos carry contribution scope and **no README excerpt**, and `skills/source-extraction/SKILL.md` gains a rule that a contribution is evidence of that contribution, never of authorship. **Fix 4 (budget):** README + languages fetched only for owned/org repos, external repos sorted by contribution count (not `updated`) under a separate `MAX_EXTERNAL_REPOS`, single search page, 403/429 degrades to owned+org with a WARNING rather than failing the ingest. **Out of scope:** private/internal org repos — reachable only with the *user's own* token (`GET /user/repos?affiliation=...`), and `/ingest` takes a bare `github_username` against an operator credential, so caller-supplied tokens are deferred as a credential-handling decision. New env `GITHUB_INCLUDE_CONTRIBUTIONS=true` + `GITHUB_MAX_EXTERNAL_REPOS=15`; API contract unchanged | PLAN.md (Phase 1.f) | None yet — design only. Planned when implemented: extended tests/unit/test_github_client.py over the existing `httpx.MockTransport` (mixed owned/org `type=all` payload, `/users/{u}/orgs`, a `/search/issues` page; asserts the three section headings, org attribution, external repo shows merged-PR count + titles but **no README excerpt**, forks still excluded, 403-on-search still returns owned repos, and a token-present case stubbing the GraphQL POST) and extended test_extraction.py (an external-repo section does not yield a `Project` attributed to the user). Unit suite unchanged (93 passed) — no code touched |
| 2026-07-21 | Phase 1.e — null-tolerant extraction schema + item-level salvage | Stop a handful of empty fields in one source from discarding an entire ingest: a real `POST /ingest` (two CVs + `github_username=thomas-choi`) died with HTTP 500 `3 validation errors for SourceExtraction / projects.{11,13,27}.description: Input should be a valid string [input_value=None]` | The skill and the schema contradicted each other. `skills/source-extraction/SKILL.md` tells the model "if a field is absent from the document, leave it empty/null" (the anti-fabrication guarantee), but `Project.description` was a required non-nullable `str`. GitHub repos without a repo description contribute no `Description:` line (`src/tools/github_client.py:59-60`), so the extractor correctly emitted `null` — and Pydantic rejected the **whole** `SourceExtraction`. Because one `github_username` produces exactly one `SourceDocument` holding all 30 repos, and `extract_source` had no error handling, 3 missing descriptions discarded a 30-repo extraction **and** both parsed CVs. The same latent trap existed on every other required `str` in the extraction-facing models | **Fix 1 (root cause):** module-level `mode="before"` validators in `src/models/schemas.py` (`_blank_if_none`/`_empty_if_none`, exposed as `NullableStr`/`NullableList` annotated types) applied to the fields the extractor can legitimately null out — `Project.{name,description,source,technologies}` (description also gains a `= ""` default), `Experience.{company,title,source,bullets}`, `Skill.{name,category}`, and the four `JobRequirements` list fields (same failure mode on a sparse posting). `TailoredCV`/`ValidationFlag`/`ValidationResult`/`CoverLetter` deliberately stay strict — a null there is a real bug. `SKILL.md` **not** changed: the schema is what should yield. **Fix 1b (ripple):** `synthesis.build_raw_source_map` now skips falsy `description`/`bullet`/`skill.name`, since once descriptions can be `""` every description-less project would collide on a single `""` key in the map the anti-fabrication gate reads (`src/agents/validation.py:73`). **Fix 2 (item-level salvage):** `extract_one` switched to `with_structured_output(SourceExtraction, include_raw=True)`, which surfaces the `ValidationError` in `{"parsed","raw","parsing_error"}` rather than raising; happy path unchanged, failure path rebuilds the extraction from `raw.tool_calls[0]["args"]` field by field, validating `experiences`/`projects`/`skills` one element at a time and dropping only the failures (each logged at WARNING with list index, `name`, pydantic message) — for this bug that keeps 27 repos and drops 3. Nothing usable (no tool call, unparseable args, every item rejected, or salvaged fields still invalid) → **re-raise the original error**; a silently empty profile is worse than a 500. The strict schema stays the tool schema handed to the model (it steers the output); leniency lives only on the error path. **Fix 2b (coarse net):** `extract_source` wraps the per-source call so a hard failure logs the source id and continues, raising only if no source survives — explicitly *not* what saves the repos. No config/env change; API contract unchanged (`/ingest` simply stops 500-ing) | src/models/schemas.py, src/agents/extraction.py, src/agents/synthesis.py, src/agents/ingestion_graph.py, tests/conftest.py, TECHNICAL-DESIGN.md §4 + §13, PRODUCT-GUIDE.md, API-REFERENCE.md (no-API-change note), OPERATIONS.md (no-setup-change note), PLAN.md | `tests/conftest.py`: `FakeLLM.with_structured_output` accepts `include_raw` and wraps responses in the envelope; new `RawMessage` tool-call stub; a dict response passes through to simulate a parse failure. `test_schemas.py` +5 (null description/name/lists → `""`/`[]` across Project/Experience/Skill/JobRequirements; `TailoredCV` still rejects null). `test_extraction.py` +5 (regression: the exact 30-project payload with `description=None` at 11/13/27 → all 30 survive with `""` and source ids still enforced; a malformed project dropped while siblings survive, with index+name logged; total salvage failure re-raises; no tool call re-raises; `include_raw=True` requested). `test_synthesis.py` +1 (two description-less projects don't collide; no `""` key). `test_graphs.py` +2 (one dead source skipped, run completes; all-dead raises). Full unit suite **93 passed** (was 80), no regressions across Phases 1–1.d. Live re-run of the failing curl still pending |
| 2026-07-20 | Design + roadmap Phase 1.d — merge previous ingests into a new profile version (`POST /merge`) — **design only, not implemented** | Provide a way to combine two or more prior ingest runs into one accumulated profile version, since re-ingesting is last-write-wins (a version is built only from that run's sources) | `synthesize_profile` only ever sees the current run's extractions and never the prior stored profile, so there is no path to union past runs; user requested an explicit merge that reuses the archived per-run outputs and generates a new profile/version | Documented (no code change): dedicated `POST /merge` (`{run_ids: list[str]≥2, profile_id?}`) that loads each run's `data/output/{run_id}/output.json` (new `run_store.load_output`), merges the snapshots via a reused-synthesis node (`merge_profiles`, `SYNTHESIS_MODEL` + `profile-synthesis` skill + a merge USER-prompt variant) that dedupes and surfaces cross-profile conflicts (deterministic list-union rejected — would duplicate jobs and drop conflict surfacing), rebuilds `raw_source_map` and preserves each entry's `source`; stored as a new version of the target `profile_id` (Phase 1.c `save_profile`) with a fresh merge `run_id` whose manifest records `merged_from: [run_ids]`. Chose a dedicated endpoint over an `/ingest` mode (no upload; inputs are prior runs by id) and snapshot-merge over raw re-extraction (per user) | PLAN.md (Phase 1.d), TECHNICAL-DESIGN.md §13 (Merge flow + Mermaid) | None yet — design only. Planned when implemented: tests/unit/test_merge.py (dedupe + conflict surfacing, raw_source_map rebuild, source preserved), extended test_api.py (POST /merge new version; 404 unknown run_id; 400 <2 ids / invalid profile_id), extended test_run_store.py (load_output roundtrip, merged_from manifest). Unit suite unchanged (80 passed) — no code touched |
| 2026-07-20 | Add optional `profile_id` form param to `POST /ingest` | Let a caller direct an ingest into a specific profile (append a new version to an existing profile, or create one under a chosen id) instead of always minting a fresh `profile_id` | `/ingest` only ever passed `{run_id, sources}` into the ingestion graph, so `store_profile` always called `save_profile(profile, None)` → a new random `profile_id` every run; re-ingesting updated sources into the same profile was impossible via the API even though `IngestionState.profile_id` and `save_profile(profile_id=...)` versioning already supported it | Added `profile_id: str | None = Form(default=None)` to `ingest`; validated with `_validate_profile_id` against `[A-Za-z0-9_-]{1,64}` (it becomes a `data/profiles/` dir name → reject path separators/traversal, 400 on bad input) before any archiving; threaded into the graph `stream_input` only when provided so the mint-a-fresh-id default is byte-for-byte unchanged when omitted. `store_profile` → `save_profile` then appends a version to an existing id or creates a new id at v1. No schema/graph-topology change (plumbing already existed). Docstring + API-REFERENCE updated; `run_id` stays distinct from `profile_id` (one execution vs. evolving profile) | src/api/routes.py, API-REFERENCE.md | Extended tests/unit/test_api.py: FakeIngestionGraph now records its input state; `test_ingest_threads_caller_profile_id_into_graph` (profile_id reaches stream_input, response echoes it + version 2), `test_ingest_without_profile_id_lets_store_mint_one` (key absent when omitted), `test_ingest_rejects_unsafe_profile_id` (`../../etc/passwd` → 400). Full unit suite 80 passed (was 77) |
| 2026-07-20 | Phase 1.b — migrate the five agents to `SKILL.md` (FUND skills mechanism) | Move each agent's hand-tuned reasoning (extraction fact/inference rules, synthesis dedupe/conflict strategy, job-analysis decomposition, tailoring HARD RULES, anti-fabrication cross-check) out of hardcoded prompt strings into versioned, discoverable skills, reusing FUND's machinery verbatim | Reasoning lived inline in `src/chains/prompts/*_prompt.py` `SYSTEM` strings — not versioned, discoverable, or independently testable; PLAN Phase 1.b calls for a behavior-preserving retrofit onto FUND's skills mechanism | Vendored `fund_models/` as a package (`__init__.py`) and pinned `pyyaml`; consumes only `scan_skills` (`agent_base`/`AgentBase` deferred to Phase 4). Authored 5 starter skills under `skills/` (source-extraction, profile-synthesis, job-analysis, cv-tailoring, anti-fabrication), each YAML frontmatter + Markdown body seeded verbatim from the agent's prior `SYSTEM` text so day-one behavior is unchanged. New `src/agents/skills.py` adapter: `resolve_skill(name)` returns a frontmatter-stripped body (cached per `SKILLS_DIR`; `""` when the dir is missing/empty, `KeyError` on an unknown name in a populated registry), `skills_catalog()` returns the `AgentBase.get_skills_context`-format summary. Each `*_prompt.py` reduced to structural scaffolding (a `{skill}` slot + `USER` template); each node prepends `resolve_skill("<node-skill>")` into the slot (extraction keeps its runtime `{source_id}` binding; tailoring composes `cv-tailoring` + `anti-fabrication`). Structured-output nodes stay single-shot `with_structured_output`, so skills resolve deterministically by node rather than via FUND's runtime `load_skill` tool. Missing `SKILLS_DIR` degrades gracefully to the inline scaffolding. `SKILLS_DIR=./skills` added to config/.env.example. Also fixed two pre-existing typos in the uncommitted `ingestion_graph.py` (`}}` syntax error line 36, `logger.ddebug` line 65) that were blocking the whole unit suite from collecting — flagged to the user. No API/product change (internal reasoning-source refactor) — API-REFERENCE.md and PRODUCT-GUIDE.md unaffected | fund_models/__init__.py, requirements.txt, src/config.py, .env.example, src/agents/skills.py, skills/*/SKILL.md (×5), src/chains/prompts/{extraction,synthesis,job_analysis,tailoring,validation}_prompt.py, src/agents/{extraction,synthesis,job_analysis,tailoring,validation}.py, src/agents/ingestion_graph.py (typo fix), TECHNICAL-DESIGN.md §13, OPERATIONS.md, PLAN.md | New tests/unit/test_skills.py (scan finds all 5 with name/description/path; resolve strips frontmatter + raises on unknown + degrades when dir missing; catalog lists every skill); extended test_extraction.py, test_synthesis.py, test_tailoring.py (job-analysis + tailoring), test_validation.py — each asserts the resolved skill body appears in the mocked-LLM system prompt and that a missing `SKILLS_DIR` still runs (behavior-preservation). Full unit suite 77 passed (was 63). Verified `python -m fund_models.skills skills/` prints all 5 with descriptions |
| 2026-07-19 | Add per-run source & output tracking (`run_id` provenance) to `/ingest` | Give end-to-end traceability between pipeline steps — archive every raw input of a run and a copy of its output, tied to the produced profile | `/ingest` kept no record of its inputs: uploaded CVs were parsed from a temp file then deleted, GitHub/free-text `SourceDocument`s were discarded, only the final profile persisted; the two ids (`job_id` SSE-only, `profile_id` storage key) never tied raw inputs → output, so a bad run couldn't be reconstructed or audited | New `src/utils/run_store.py` (mirrors profile_store) keyed by `run_id` (= `job_id`, generated if absent): `save_source_file` sanitizes filenames (`Path(name).name`) and records size+sha256; `write_manifest`/`link_profile` maintain `sources/{run_id}/manifest.json`; `save_output` writes `output/{run_id}/output.json` and links the manifest to `profile_id`/`version`. routes.py allocates `run_id` first and archives raw CV bytes **before** parsing (parses from the stored path, replacing the temp-file-then-delete flow), GitHub→`github.json`, free_text→`linkedin/linkedin-summary.txt` (LinkedIn mapped through free_text; dedicated LinkedIn ingestion stays Phase 2), writes the manifest, threads `run_id` through `IngestionState`, and returns `run_id` in the response. `store_profile` calls `run_store.save_output`. logging_setup adds a `contextvars` `run_id` filter → `[run:<run_id>]` tag on every log line; `SourceDocument.stored_path` links a source to its archived file. Retention change: raw CVs are now retained (documented in OPERATIONS.md) | src/utils/run_store.py, src/api/routes.py, src/agents/ingestion_graph.py, src/utils/logging_setup.py, src/models/schemas.py, PLAN.md, TECHNICAL-DESIGN.md §13, OPERATIONS.md, API-REFERENCE.md, PRODUCT-GUIDE.md | New tests/unit/test_run_store.py (save/sanitize/manifest roundtrip/output+link); extended test_api.py (run_id in response, sources/{run_id}/ CV + github.json + linkedin-summary.txt + manifest.json), test_graphs.py (store_profile writes output/{run_id}/output.json), test_logging_setup.py ([run:...] tag); full unit suite 63 passed |
| 2026-07-19 | Disable DeepSeek thinking mode in the LLM factory | Make `LLM_PROVIDER=deepseek` usable — /ingest returned 500 (upstream 400) on the first extraction call | DeepSeek's current models (deepseek-v4-flash/-pro) run in thinking mode, which rejects the forced `tool_choice` that LangChain's `with_structured_output` sends ("Thinking mode does not support this tool_choice"); every pipeline stage relies on structured output, so the provider could never work. Reproduced with direct curl; forced tool calls succeed once `thinking: disabled` is passed | `make_llm` deepseek branch adds `extra_body={"thinking": {"type": "disabled"}}` to ChatDeepSeek so structured output works unchanged across all stages. (Also fixed during triage, config-only: .env LLM_BASE_URL had a typosquat-style typo `api.keepseek.com` — removed; ChatDeepSeek defaults to the real endpoint. Key sent to that domain should be rotated.) No API/product/architecture change — API-REFERENCE.md, PRODUCT-GUIDE.md, TECHNICAL-DESIGN.md unaffected (provider-compat detail, documented in OPERATIONS.md env table) | src/agents/llm.py, tests/unit/test_llm.py, OPERATIONS.md | tests/unit/test_llm.py::test_deepseek_disables_thinking (importorskip langchain_deepseek); suite 53 passed; live-verified: extract_one on a small free-text doc via deepseek-v4-flash returned populated structured extraction |
| 2026-07-19 | Document local-LLM (llama.cpp) reference setup and retune .env for it | Run the pipeline fully offline on the dev box (RTX 3090 24 GB / 32 GB RAM, Qwen3.6-35B-A3B-UD-Q4_K_M) after `/ingest` failed with `exceed_context_size_error` (prompt 4966 tokens > n_ctx 4096) | llama-server was started with the default small context window; the GitHub extraction prompt alone (~30 repos + README excerpts) exceeds 4096 tokens, and later stages (synthesis + 8k-token output budget) need far more | New OPERATIONS.md section "Local LLM via llama.cpp": reference `llama-server` command (`-c 32768 -ngl 99 -fa on -ctk q8_0 -ctv q8_0 --jinja`) with per-flag VRAM-budget reasoning (~20 GB weights leaves ~4 GB for KV/compute → 8-bit KV cache to fit 32k), OOM fallback order (`--n-cpu-moe` before shrinking context; never shrink app inputs first), thinking-variant caveat (`enable_thinking:false`), and matching `.env` block; env-var table rows for LLM_TEMPERATURE (~0.2 for local/OSS providers) and LLM_STREAM_TIMEOUT_S (300 for local) expanded; .env.example points llamacpp users at the new section; local `.env` (gitignored) updated to match. Docs + local config only — no code change; API-REFERENCE.md, PRODUCT-GUIDE.md, TECHNICAL-DESIGN.md unaffected | OPERATIONS.md, .env.example, .env (untracked) | No code change — unit suite unaffected; live-verify by restarting llama-server with the documented flags and re-running the /ingest smoke test |
| 2026-07-19 | Add DEBUG tracing to the GitHub ingestion path | Let the operator watch in the log file how a user's GitHub content flows through fetch → extraction → synthesis (investigating nvidia-provider empty-profile ingests) | Pipeline modules had no debug logging, so LOG_LEVEL=DEBUG revealed nothing about what content the LLM stages received or returned | `logger.debug` calls in src/tools/github_client.py (non-fork repo list, per-repo language/stars/readme, full assembled source document), src/agents/extraction.py (input size/model, per-category result counts, full extraction JSON), src/agents/synthesis.py (extraction count/payload size/model, profile summary counts, full profile JSON); logging_setup caps httpx/httpcore/urllib3/watchfiles/openai at INFO so DEBUG runs stay readable; .env LOG_LEVEL set to DEBUG. No API/product/architecture change — API-REFERENCE.md, PRODUCT-GUIDE.md, TECHNICAL-DESIGN.md unaffected (log statements only) | src/tools/github_client.py, src/agents/extraction.py, src/agents/synthesis.py, src/utils/logging_setup.py, OPERATIONS.md | Suite 52 passed (log calls exercised by existing mocked-LLM tests); live-verified: fetch_github_profile("thomas-choi") wrote repo-level + full-document DEBUG lines to logs/app.log |
| 2026-07-19 | Add env-configurable logging (LOG_LEVEL, LOG_FILE) | Persist app + request logs to a file with a tunable level, configured from .env | Only ad-hoc `logging.getLogger` calls existed; nothing configured the root logger, so provider/agent logs went wherever uvicorn's console defaults sent them and nothing was written to disk | New `src/utils/logging_setup.py::setup_logging()` (idempotent): root level from LOG_LEVEL (fallback INFO on unknown names), console handler always, RotatingFileHandler (10 MB × 3 backups, parent dir auto-created) when LOG_FILE is set; uvicorn/uvicorn.access/uvicorn.error handlers cleared and propagated to root so access logs reach the file; called from `create_app()` which logs a startup line. .env.example + .gitignore (`logs/`) updated. No API/product/architecture change — API-REFERENCE.md, PRODUCT-GUIDE.md, TECHNICAL-DESIGN.md unaffected (operational infra only, documented in OPERATIONS.md) | src/utils/logging_setup.py, src/config.py, src/api/main.py, .env.example, .gitignore, OPERATIONS.md | tests/unit/test_logging_setup.py (level+file writes, console-only, idempotency, unknown-level fallback); suite 52 passed; live-verified startup/access lines in logs/app.log via /healthz |
| 2026-07-18 | Add LLM provider packages to requirements.txt | `/ingest` with `LLM_PROVIDER=nvidia` returned 500 "No module named 'langchain_nvidia_ai_endpoints'" | `make_llm` imports the provider package lazily at request time, but requirements.txt only shipped langchain-anthropic, so non-Anthropic providers failed on first LLM call | Added langchain-nvidia-ai-endpoints, langchain-openai, langchain-google-genai, langchain (>= pins) to requirements.txt and installed into the venv; no code change — the lazy-import factory picks them up on the next request. Known issue found while verifying: meta/llama-3.3-70b-instruct via the NVIDIA endpoint returns an *empty* structured extraction on the full ~20K-char GitHub source doc (small docs extract fine), so nvidia-provider ingests currently store an empty profile — model/provider capability issue, not a pipeline bug. No API/product/architecture change — API-REFERENCE.md, PRODUCT-GUIDE.md, TECHNICAL-DESIGN.md unaffected | requirements.txt, OPERATIONS.md | Verified live: /ingest 200 with nvidia provider; direct extract_one call confirmed package import + structured output path. Unit suite mocks make_llm, so no new tests |
| 2026-07-18 | Document LAN access (--host 0.0.0.0) | Accessing /docs from another host was refused | `uvicorn --reload` binds 127.0.0.1 by default; docs only showed the loopback command | Added the `--host 0.0.0.0 --port 8000` variant to README and OPERATIONS (Docker Compose already binds 0.0.0.0). Docs-only — no code/API change | README.md, OPERATIONS.md | No code change — suite unaffected (48 passed) |
| 2026-07-18 | Redirect / to /docs | Browser visits to http://127.0.0.1:8000 returned 404 instead of something useful | API had no root route; only REST endpoints were registered | `GET /` (excluded from schema) returns a 307 RedirectResponse to FastAPI's built-in Swagger UI at /docs. No graph/product change — TECHNICAL-DESIGN.md and PRODUCT-GUIDE.md unaffected | src/api/main.py, API-REFERENCE.md | tests/unit/test_api.py::test_root_redirects_to_docs; suite 48 passed |
| 2026-07-18 | Write README overview/setup/usage | Give the repo a proper front page covering what the system does, setup, and operation | README was a two-line stub from the initial scaffold | Overview with Mermaid pipeline diagram and guardrail summary; local + Docker setup; curl walkthrough of the ingest→review→tailor flow; test commands; doc index table linking the detailed docs. Docs-only change — no code, API, or behavior affected (TECHNICAL-DESIGN/OPERATIONS/API-REFERENCE/PRODUCT-GUIDE unchanged; README summarizes them) | README.md | No code change — existing suite (47 passed) unaffected |
| 2026-07-18 | Rework LLM factory to FUND's get_llm() method | Align `make_llm` with the user's existing FUND-project pattern (provider-switched, env-configured factory) | Phase 1 factory was Anthropic-only; user requested the same method as FUND-financial-Research `fund_models/agent_base.py:get_llm()` | Provider switch with lazy imports (anthropic default, openai, google, nvidia, llamacpp, deepseek, openrouter) configured via LLM_PROVIDER / LLM_API_KEY (falls back to ANTHROPIC_API_KEY) / LLM_TEMPERATURE / LLM_MAX_TOKENS / LLM_BASE_URL / LLM_STREAM_TIMEOUT_S; kwargs logged with api key masked; `model`/`max_tokens` stay per-call args for per-stage tiering; deviation from FUND: temperature only sent when explicitly set because current Claude models reject non-default sampling params. No API/product change — API-REFERENCE.md and PRODUCT-GUIDE.md unaffected (internal factory refactor only) | src/agents/llm.py, src/config.py, .env.example, OPERATIONS.md, TECHNICAL-DESIGN.md §13 | New tests/unit/test_llm.py (provider construction, max_tokens default, temperature omission, unsupported provider, key masking); full suite 47 passed |
| 2026-07-18 | Implement Phase 1: core pipeline + FastAPI + Docker | Working ingest→profile→tailor→validate pipeline per PLAN.md Phase 1 (design doc §12 steps 1–4) | New feature — repo previously contained only docs | Two LangGraph StateGraphs (ingestion: ingest_sources→extract_source→synthesize_profile→store_profile; tailoring: analyze_job→tailor_cv→validate_cv); Pydantic schemas per design §4/§6/§7 + ValidationResult + CareerProfile.conflicts; deterministic docx/pdf/GitHub ingestion tools; model tiering Haiku/Sonnet via env vars; structured output everywhere via `with_structured_output`; deterministic raw_source_map + layered validation gate (source-map hit → difflib similarity → LLM cross-check); versioned JSON profile store (data/profiles/{id}/v{n}.json + latest pointer); FastAPI endpoints /ingest (+SSE per-node progress via in-process job registry), /profile GET/PUT, /tailor, /healthz; Dockerfile (python:3.11-slim) + docker-compose (one service, .env, data/ volume) | requirements.txt, .env.example, pytest.ini, Dockerfile, docker-compose.yml, src/config.py, src/models/schemas.py, src/tools/{docx_reader,pdf_reader,github_client}.py, src/agents/{llm,extraction,synthesis,job_analysis,tailoring,validation,ingestion_graph,tailoring_graph}.py, src/chains/prompts/*.py, src/utils/profile_store.py, src/api/{main,routes}.py, TECHNICAL-DESIGN.md §13, OPERATIONS.md, API-REFERENCE.md, PRODUCT-GUIDE.md, CLAUDE.md | 42 unit tests (tests/unit/: schemas, docx/pdf readers with generated fixtures, GitHub client via httpx.MockTransport, extraction/synthesis/tailoring with mocked LLMs, validation key suite incl. fabricated-bullet-flagged / reworded-passes / unsourced-skill-flagged, profile store versioning, graphs with mocked nodes, API via TestClient incl. SSE) + tests/integration/test_pipeline.py (real API, `pytest -m integration`); Docker image built and /healthz smoke-tested |
