# History

Newest first. Never delete or rewrite past entries — only append new ones at the top.

| Date | Action | Goal | Root Cause | Implementation Detail | Related Files | Test Coverage |
|---|---|---|---|---|---|---|
| 2026-07-19 | Add per-run source & output tracking (`run_id` provenance) to `/ingest` | Give end-to-end traceability between pipeline steps — archive every raw input of a run and a copy of its output, tied to the produced profile | `/ingest` kept no record of its inputs: uploaded CVs were parsed from a temp file then deleted, GitHub/free-text `SourceDocument`s were discarded, only the final profile persisted; the two ids (`job_id` SSE-only, `profile_id` storage key) never tied raw inputs → output, so a bad run couldn't be reconstructed or audited | New `src/utils/run_store.py` (mirrors profile_store) keyed by `run_id` (= `job_id`, generated if absent): `save_source_file` sanitizes filenames (`Path(name).name`) and records size+sha256; `write_manifest`/`link_profile` maintain `sources/{run_id}/manifest.json`; `save_output` writes `output/{run_id}/output.json` and links the manifest to `profile_id`/`version`. routes.py allocates `run_id` first and archives raw CV bytes **before** parsing (parses from the stored path, replacing the temp-file-then-delete flow), GitHub→`github.json`, free_text→`linkedin/linkedin-summary.txt` (LinkedIn mapped through free_text; dedicated LinkedIn ingestion stays Phase 2), writes the manifest, threads `run_id` through `IngestionState`, and returns `run_id` in the response. `store_profile` calls `run_store.save_output`. logging_setup adds a `contextvars` `run_id` filter → `[run:<run_id>]` tag on every log line; `SourceDocument.stored_path` links a source to its archived file. Retention change: raw CVs are now retained (documented in OPERATIONS.md) | src/utils/run_store.py, src/api/routes.py, src/agents/ingestion_graph.py, src/utils/logging_setup.py, src/models/schemas.py, PLAN.md, TECHNICAL-DESIGN.md §13, OPERATIONS.md, API-REFERENCE.md, PRODUCT-GUIDE.md | New tests/unit/test_run_store.py (save/sanitize/manifest roundtrip/output+link); extended test_api.py (run_id in response, sources/{run_id}/ CV + github.json + linkedin-summary.txt + manifest.json), test_graphs.py (store_profile writes output/{run_id}/output.json), test_logging_setup.py ([run:...] tag); full unit suite 63 passed |
| 2026-07-19 | Disable DeepSeek thinking mode in the LLM factory | Make `LLM_PROVIDER=deepseek` usable — /ingest returned 500 (upstream 400) on the first extraction call | DeepSeek's current models (deepseek-v4-flash/-pro) run in thinking mode, which rejects the forced `tool_choice` that LangChain's `with_structured_output` sends ("Thinking mode does not support this tool_choice"); every pipeline stage relies on structured output, so the provider could never work. Reproduced with direct curl; forced tool calls succeed once `thinking: disabled` is passed | `make_llm` deepseek branch adds `extra_body={"thinking": {"type": "disabled"}}` to ChatDeepSeek so structured output works unchanged across all stages. (Also fixed during triage, config-only: .env LLM_BASE_URL had a typosquat-style typo `api.keepseek.com` — removed; ChatDeepSeek defaults to the real endpoint. Key sent to that domain should be rotated.) No API/product/architecture change — API-REFERENCE.md, PRODUCT-GUIDE.md, TECHNICAL-DESIGN.md unaffected (provider-compat detail, documented in OPERATIONS.md env table) | src/agents/llm.py, tests/unit/test_llm.py, OPERATIONS.md | tests/unit/test_llm.py::test_deepseek_disables_thinking (importorskip langchain_deepseek); suite 53 passed; live-verified: extract_one on a small free-text doc via deepseek-v4-flash returned populated structured extraction |
| 2026-07-19 | Document local-LLM (llama.cpp) reference setup and retune .env for it | Run the pipeline fully offline on the dev box (RTX 3090 24 GB / 32 GB RAM, Qwen3.6-35B-A3B-UD-Q4_K_M) after `/ingest` failed with `exceed_context_size_error` (prompt 4966 tokens > n_ctx 4096) | llama-server was started with the default small context window; the GitHub extraction prompt alone (~30 repos + README excerpts) exceeds 4096 tokens, and later stages (synthesis + 8k-token output budget) need far more | New OPERATIONS.md section "Local LLM via llama.cpp": reference `llama-server` command (`-c 32768 -ngl 99 -fa on -ctk q8_0 -ctv q8_0 --jinja`) with per-flag VRAM-budget reasoning (~20 GB weights leaves ~4 GB for KV/compute → 8-bit KV cache to fit 32k), OOM fallback order (`--n-cpu-moe` before shrinking context; never shrink app inputs first), thinking-variant caveat (`enable_thinking:false`), and matching `.env` block; env-var table rows for LLM_TEMPERATURE (~0.2 for local/OSS providers) and LLM_STREAM_TIMEOUT_S (300 for local) expanded; .env.example points llamacpp users at the new section; local `.env` (gitignored) updated to match. Docs + local config only — no code change; API-REFERENCE.md, PRODUCT-GUIDE.md, TECHNICAL-DESIGN.md unaffected | OPERATIONS.md, .env.example, .env (untracked) | No code change — unit suite unaffected; live-verify by restarting llama-server with the documented flags and re-running the /ingest smoke test |
| 2026-07-19 | Add DEBUG tracing to the GitHub ingestion path | Let the operator watch in the log file how a user's GitHub content flows through fetch → extraction → synthesis (investigating nvidia-provider empty-profile ingests) | Pipeline modules had no debug logging, so LOG_LEVEL=DEBUG revealed nothing about what content the LLM stages received or returned | `logger.debug` calls in src/tools/github_client.py (non-fork repo list, per-repo language/stars/readme, full assembled source document), src/agents/extraction.py (input size/model, per-category result counts, full extraction JSON), src/agents/synthesis.py (extraction count/payload size/model, profile summary counts, full profile JSON); logging_setup caps httpx/httpcore/urllib3/watchfiles/openai at INFO so DEBUG runs stay readable; .env LOG_LEVEL set to DEBUG. No API/product/architecture change — API-REFERENCE.md, PRODUCT-GUIDE.md, TECHNICAL-DESIGN.md unaffected (log statements only) | src/tools/github_client.py, src/agents/extraction.py, src/agents/synthesis.py, src/utils/logging_setup.py, OPERATIONS.md | Suite 52 passed (log calls exercised by existing mocked-LLM tests); live-verified: fetch_github_profile("thomas-choi") wrote repo-level + full-document DEBUG lines to logs/app.log |
| 2026-07-19 | Add env-configurable logging (LOG_LEVEL, LOG_FILE) | Persist app + request logs to a file with a tunable level, configured from .env | Only ad-hoc `logging.getLogger` calls existed; nothing configured the root logger, so provider/agent logs went wherever uvicorn's console defaults sent them and nothing was written to disk | New `src/utils/logging_setup.py::setup_logging()` (idempotent): root level from LOG_LEVEL (fallback INFO on unknown names), console handler always, RotatingFileHandler (10 MB × 3 backups, parent dir auto-created) when LOG_FILE is set; uvicorn/uvicorn.access/uvicorn.error handlers cleared and propagated to root so access logs reach the file; called from `create_app()` which logs a startup line. .env.example + .gitignore (`logs/`) updated. No API/product/architecture change — API-REFERENCE.md, PRODUCT-GUIDE.md, TECHNICAL-DESIGN.md unaffected (operational infra only, documented in OPERATIONS.md) | src/utils/logging_setup.py, src/config.py, src/api/main.py, .env.example, .gitignore, OPERATIONS.md | tests/unit/test_logging_setup.py (level+file writes, console-only, idempotency, unknown-level fallback); suite 52 passed; live-verified startup/access lines in logs/app.log via /healthz |
| 2026-07-18 | Add LLM provider packages to requirements.txt | `/ingest` with `LLM_PROVIDER=nvidia` returned 500 "No module named 'langchain_nvidia_ai_endpoints'" | `make_llm` imports the provider package lazily at request time, but requirements.txt only shipped langchain-anthropic, so non-Anthropic providers failed on first LLM call | Added langchain-nvidia-ai-endpoints, langchain-openai, langchain-google-genai, langchain (>= pins) to requirements.txt and installed into the venv; no code change — the lazy-import factory picks them up on the next request. Known issue found while verifying: meta/llama-3.3-70b-instruct via the NVIDIA endpoint returns an *empty* structured extraction on the full ~20K-char GitHub source doc (small docs extract fine), so nvidia-provider ingests currently store an empty profile — model/provider capability issue, not a pipeline bug. No API/product/architecture change — API-REFERENCE.md, PRODUCT-GUIDE.md, TECHNICAL-DESIGN.md unaffected | requirements.txt, OPERATIONS.md | Verified live: /ingest 200 with nvidia provider; direct extract_one call confirmed package import + structured output path. Unit suite mocks make_llm, so no new tests |
| 2026-07-18 | Document LAN access (--host 0.0.0.0) | Accessing /docs from another host was refused | `uvicorn --reload` binds 127.0.0.1 by default; docs only showed the loopback command | Added the `--host 0.0.0.0 --port 8000` variant to README and OPERATIONS (Docker Compose already binds 0.0.0.0). Docs-only — no code/API change | README.md, OPERATIONS.md | No code change — suite unaffected (48 passed) |
| 2026-07-18 | Redirect / to /docs | Browser visits to http://127.0.0.1:8000 returned 404 instead of something useful | API had no root route; only REST endpoints were registered | `GET /` (excluded from schema) returns a 307 RedirectResponse to FastAPI's built-in Swagger UI at /docs. No graph/product change — TECHNICAL-DESIGN.md and PRODUCT-GUIDE.md unaffected | src/api/main.py, API-REFERENCE.md | tests/unit/test_api.py::test_root_redirects_to_docs; suite 48 passed |
| 2026-07-18 | Write README overview/setup/usage | Give the repo a proper front page covering what the system does, setup, and operation | README was a two-line stub from the initial scaffold | Overview with Mermaid pipeline diagram and guardrail summary; local + Docker setup; curl walkthrough of the ingest→review→tailor flow; test commands; doc index table linking the detailed docs. Docs-only change — no code, API, or behavior affected (TECHNICAL-DESIGN/OPERATIONS/API-REFERENCE/PRODUCT-GUIDE unchanged; README summarizes them) | README.md | No code change — existing suite (47 passed) unaffected |
| 2026-07-18 | Rework LLM factory to FUND's get_llm() method | Align `make_llm` with the user's existing FUND-project pattern (provider-switched, env-configured factory) | Phase 1 factory was Anthropic-only; user requested the same method as FUND-financial-Research `fund_models/agent_base.py:get_llm()` | Provider switch with lazy imports (anthropic default, openai, google, nvidia, llamacpp, deepseek, openrouter) configured via LLM_PROVIDER / LLM_API_KEY (falls back to ANTHROPIC_API_KEY) / LLM_TEMPERATURE / LLM_MAX_TOKENS / LLM_BASE_URL / LLM_STREAM_TIMEOUT_S; kwargs logged with api key masked; `model`/`max_tokens` stay per-call args for per-stage tiering; deviation from FUND: temperature only sent when explicitly set because current Claude models reject non-default sampling params. No API/product change — API-REFERENCE.md and PRODUCT-GUIDE.md unaffected (internal factory refactor only) | src/agents/llm.py, src/config.py, .env.example, OPERATIONS.md, TECHNICAL-DESIGN.md §13 | New tests/unit/test_llm.py (provider construction, max_tokens default, temperature omission, unsupported provider, key masking); full suite 47 passed |
| 2026-07-18 | Implement Phase 1: core pipeline + FastAPI + Docker | Working ingest→profile→tailor→validate pipeline per PLAN.md Phase 1 (design doc §12 steps 1–4) | New feature — repo previously contained only docs | Two LangGraph StateGraphs (ingestion: ingest_sources→extract_source→synthesize_profile→store_profile; tailoring: analyze_job→tailor_cv→validate_cv); Pydantic schemas per design §4/§6/§7 + ValidationResult + CareerProfile.conflicts; deterministic docx/pdf/GitHub ingestion tools; model tiering Haiku/Sonnet via env vars; structured output everywhere via `with_structured_output`; deterministic raw_source_map + layered validation gate (source-map hit → difflib similarity → LLM cross-check); versioned JSON profile store (data/profiles/{id}/v{n}.json + latest pointer); FastAPI endpoints /ingest (+SSE per-node progress via in-process job registry), /profile GET/PUT, /tailor, /healthz; Dockerfile (python:3.11-slim) + docker-compose (one service, .env, data/ volume) | requirements.txt, .env.example, pytest.ini, Dockerfile, docker-compose.yml, src/config.py, src/models/schemas.py, src/tools/{docx_reader,pdf_reader,github_client}.py, src/agents/{llm,extraction,synthesis,job_analysis,tailoring,validation,ingestion_graph,tailoring_graph}.py, src/chains/prompts/*.py, src/utils/profile_store.py, src/api/{main,routes}.py, TECHNICAL-DESIGN.md §13, OPERATIONS.md, API-REFERENCE.md, PRODUCT-GUIDE.md, CLAUDE.md | 42 unit tests (tests/unit/: schemas, docx/pdf readers with generated fixtures, GitHub client via httpx.MockTransport, extraction/synthesis/tailoring with mocked LLMs, validation key suite incl. fabricated-bullet-flagged / reworded-passes / unsourced-skill-flagged, profile store versioning, graphs with mocked nodes, API via TestClient incl. SSE) + tests/integration/test_pipeline.py (real API, `pytest -m integration`); Docker image built and /healthz smoke-tested |
